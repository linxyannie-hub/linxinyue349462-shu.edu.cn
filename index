<!DOCTYPE html><html lang="zh-CN"><head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>直渠握手优质代理推荐工作台</title>
<style>/* ============================================================
   AgentMatch v3 设计系统 —— 「晨雾工作室」柔和亮色工作台
   视觉命题：清晨落地窗边的工作室——暖白纸面上漂着几团
   若有似无的青雾与霞光，信息像卡片一样轻轻浮起来，
   而不是钉在仪表盘上。
   设计原则：呼吸感 > 密度；每一次交互都有柔和的物理反馈；
   动效讲"浮现与生长"，不讲"闪烁与跳动"。
   记忆点（唯一）：证据优先推荐卡 + 四色匹配分生长条。
   ============================================================ */

:root {
  --bg-0: #eef1f4;              /* 冷调玻璃底 */
  --card: rgba(255,255,255,.46);
  --card-solid: rgba(255,255,255,.72);
  --line: rgba(255,255,255,.55);
  --line-strong: rgba(38,50,80,.14);

  --glass-blur: 22px;
  --glass-border: 1px solid rgba(255,255,255,.55);
  --glass-inner: inset 0 1px 0 rgba(255,255,255,.75), inset 0 -1px 0 rgba(255,255,255,.18);
  --glass-shadow: 0 8px 32px rgba(43,49,64,.10), 0 1.5px 6px rgba(43,49,64,.05);

  --tx-1: #2b3140;
  --tx-2: #5b6478;
  --tx-3: #8e97ab;

  --cy: #12a5b8;                /* 玻璃青：主强调 */
  --cy-deep: #0b8a9c;
  --cy-soft: rgba(18,165,184,.12);
  --gold: #e8960c;
  --gold-soft: rgba(232,150,12,.12);
  --rose: #e8604f;
  --rose-soft: rgba(232,96,79,.10);
  --blue: #5b7cfa;
  --blue-soft: rgba(91,124,250,.12);
  --violet: #8b6ef0;

  --seg-case: #f0796a;
  --seg-cert: #7d95fb;
  --seg-cap: #2cbcb4;
  --seg-budget: #f0b04a;

  --r-sm: 12px; --r-md: 16px; --r-lg: 24px;
  --shadow-1: var(--glass-shadow);
  --shadow-2: 0 16px 48px rgba(43,49,64,.14), 0 4px 12px rgba(43,49,64,.06);
  --shadow-cy: 0 8px 28px rgba(18,165,184,.28);
  --font-num: "SF Mono", ui-monospace, "JetBrains Mono", Menlo, monospace;
  --spring: cubic-bezier(.34,1.4,.44,1);
  --ease: cubic-bezier(.22,.8,.36,1);
  --fluid: cubic-bezier(.4,0,.2,1);
}

* { box-sizing: border-box; margin: 0; padding: 0; }
html { -webkit-font-smoothing: antialiased; }
body {
  font-family: -apple-system, "PingFang SC", "Noto Sans SC", "Microsoft YaHei", sans-serif;
  background: var(--bg-0);
  color: var(--tx-1);
  font-size: 14px; line-height: 1.7;
  display: grid; grid-template-columns: 232px 1fr;
  min-height: 100vh;
  position: relative;
}

/* 液态光团：饱和流动背景，给玻璃提供折射内容 */
body::before, body::after {
  content: ""; position: fixed; z-index: 0; pointer-events: none;
  width: 64vw; height: 64vw; border-radius: 50%;
  filter: blur(80px); opacity: .8;
}
body::before {
  background: radial-gradient(circle at 30% 30%, rgba(20,190,180,.30), transparent 58%),
              radial-gradient(circle at 72% 58%, rgba(101,134,255,.26), transparent 55%),
              radial-gradient(circle at 50% 80%, rgba(255,120,190,.14), transparent 60%);
  top: -20vw; right: -16vw;
  animation: drift1 22s ease-in-out infinite alternate;
}
body::after {
  background: radial-gradient(circle at 60% 40%, rgba(255,180,80,.24), transparent 55%),
              radial-gradient(circle at 28% 68%, rgba(140,110,250,.20), transparent 58%),
              radial-gradient(circle at 80% 20%, rgba(80,210,255,.16), transparent 55%);
  bottom: -24vw; left: -12vw;
  animation: drift2 28s ease-in-out infinite alternate;
}
@keyframes drift1 { from { transform: translate(0,0) scale(1) rotate(0deg); } to { transform: translate(-7vw,9vh) scale(1.18) rotate(8deg); } }
@keyframes drift2 { from { transform: translate(0,0) scale(1.1) rotate(0deg); } to { transform: translate(8vw,-7vh) scale(1) rotate(-6deg); } }

b, strong { font-weight: 650; }
button { font: inherit; cursor: pointer; color: inherit; }
svg text { font-family: inherit; }

/* 入场动效族 */
@keyframes rise { from { opacity: 0; transform: translateY(14px); } to { opacity: 1; transform: none; } }
@keyframes cardIn { from { opacity: 0; transform: translateY(18px) scale(.985); } to { opacity: 1; transform: none; } }
@keyframes growX { from { transform: scaleX(0); } to { transform: scaleX(1); } }
@keyframes popIn { from { opacity: 0; transform: scale(.86); } to { opacity: 1; transform: scale(1); } }
@keyframes breathe { 0%,100% { box-shadow: 0 6px 24px rgba(20,168,160,.16); } 50% { box-shadow: 0 6px 34px rgba(20,168,160,.3); } }
@media (prefers-reduced-motion: reduce) {
  *, body::before, body::after { animation: none !important; transition: none !important; }
}

/* ---------------- 侧导航 ---------------- */
.sidenav {
  background: linear-gradient(160deg, rgba(255,255,255,.55), rgba(255,255,255,.32));
  backdrop-filter: blur(var(--glass-blur)) saturate(1.6); -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.6);
  border-right: var(--glass-border);
  box-shadow: var(--glass-inner);
  padding: 26px 16px;
  display: flex; flex-direction: column; gap: 26px;
  position: sticky; top: 0; height: 100vh; z-index: 10;
}
.logo { display: flex; gap: 11px; align-items: center; padding: 0 8px; }
.logo-mark {
  width: 40px; height: 40px; border-radius: 13px; flex: none;
  display: grid; place-items: center;
  font-weight: 800; font-size: 18px; color: #fff;
  background: linear-gradient(135deg, #17b3aa, #5b7cfa);
  box-shadow: var(--shadow-cy);
}
.logo-text { line-height: 1.3; }
.logo-text b { font-size: 16.5px; letter-spacing: .2px; display: block; }
.logo-text span { font-size: 11px; color: var(--tx-3); }

.nav-list { display: flex; flex-direction: column; gap: 5px; }
.nav-list button {
  display: grid; grid-template-columns: 28px 1fr; grid-template-rows: auto auto;
  text-align: left; gap: 0 9px;
  border: none; background: none;
  padding: 10px 12px; border-radius: var(--r-md);
  color: var(--tx-2); transition: all .22s var(--ease);
}
.nav-list button i {
  grid-row: span 2; font-style: normal; font-size: 17px;
  display: grid; place-items: center; color: var(--tx-3);
  transition: transform .25s var(--spring);
}
.nav-list button em { font-style: normal; font-size: 11px; color: var(--tx-3); grid-column: 2; }
.nav-list button:hover { background: rgba(43,49,64,.05); color: var(--tx-1); }
.nav-list button:hover i { transform: translateY(-1px) scale(1.1); }
.nav-list button.active {
  background: linear-gradient(145deg, rgba(255,255,255,.85), rgba(255,255,255,.55));
  color: var(--tx-1);
  box-shadow: var(--glass-shadow), var(--glass-inner), inset 3px 0 0 var(--cy);
  border: 1px solid rgba(255,255,255,.7);
}
.nav-list button.active i { color: var(--cy); }

.nav-foot { margin-top: auto; display: flex; flex-direction: column; gap: 13px; }
.compliance-mini {
  font-size: 11px; color: var(--tx-3); line-height: 1.75;
  border: 1px dashed var(--line-strong); border-radius: var(--r-md); padding: 11px 13px;
  background: rgba(255,255,255,.5);
}
.compliance-mini b { color: var(--cy-deep); display: block; font-size: 11.5px; }
.author-badge {
  font-size: 11px; text-align: center; padding: 6px 10px; border-radius: 999px;
  background: var(--cy-soft); color: var(--cy-deep); border: 1px solid rgba(20,168,160,.25);
}

/* ---------------- 主区 ---------------- */
.main {
  padding: 34px 44px 80px;
  min-width: 0;
  width: 100%;
  max-width: 1560px;
  position: relative;
  z-index: 1;
}
.page { animation: rise .45s var(--ease) both; }

.page-head { display: flex; justify-content: space-between; align-items: flex-end; gap: 20px; margin-bottom: 26px; flex-wrap: wrap; }
.page-head h1 { font-size: 26px; letter-spacing: .2px; font-weight: 700; }
.page-head p { color: var(--tx-2); margin-top: 5px; }
.hl-num { color: var(--cy-deep); font-family: var(--font-num); font-size: 16px; }
.wave { display: inline-block; animation: waveHand 2.4s ease-in-out 1 .6s; transform-origin: 72% 78%; }
@keyframes waveHand { 0%,100% { transform: rotate(0); } 15% { transform: rotate(16deg); } 30% { transform: rotate(-7deg); } 45% { transform: rotate(14deg); } 60% { transform: rotate(-4deg); } 75% { transform: rotate(8deg); } }

.btn {
  border: 1px solid rgba(255,255,255,.65);
  background: linear-gradient(150deg, rgba(255,255,255,.72), rgba(255,255,255,.44));
  backdrop-filter: blur(12px) saturate(1.4); -webkit-backdrop-filter: blur(12px) saturate(1.4);
  border-radius: var(--r-md); padding: 8.5px 19px; font-size: 13.5px; font-weight: 600;
  color: var(--tx-1);
  transition: all .25s var(--spring);
  box-shadow: 0 1px 2px rgba(43,49,64,.04);
}
.btn:hover { border-color: var(--cy); color: var(--cy-deep); transform: translateY(-1.5px); box-shadow: 0 5px 14px rgba(20,168,160,.14); }
.btn:active { transform: translateY(0) scale(.98); }
.btn:disabled { opacity: .4; cursor: not-allowed; transform: none !important; box-shadow: none; }
.btn-primary {
  background: linear-gradient(135deg, #17b8c9, #0e96ab 55%, #5b7cfa 140%); color: #fff; border: none;
  box-shadow: var(--shadow-cy), inset 0 1px 0 rgba(255,255,255,.45), inset 0 -1px 0 rgba(0,0,0,.08);
  position: relative; overflow: hidden;
}
.btn-primary::after {
  content: ""; position: absolute; inset: 0;
  background: linear-gradient(105deg, transparent 30%, rgba(255,255,255,.35) 48%, transparent 62%);
  transform: translateX(-120%); transition: transform .6s var(--fluid);
}
.btn-primary:hover::after { transform: translateX(120%); }
.btn-primary:hover { color: #fff; filter: brightness(1.05); box-shadow: 0 8px 28px rgba(20,168,160,.32); }
.btn-lg { padding: 13px 28px; font-size: 15px; border-radius: 16px; }
.btn-sm { padding: 5px 13px; font-size: 12px; }
.btn-dark-ghost { background: transparent; }
.btn-gold { background: var(--gold-soft); border-color: rgba(232,150,12,.45); color: var(--gold); }
.btn-gold:hover { border-color: var(--gold); color: var(--gold); box-shadow: 0 5px 14px rgba(232,150,12,.18); }
.btn-rose { background: var(--rose-soft); border-color: rgba(232,96,79,.4); color: var(--rose); }
.btn-cy { background: var(--cy-soft); border-color: rgba(20,168,160,.35); color: var(--cy-deep); }

.panel {
  background: linear-gradient(155deg, rgba(255,255,255,.58), rgba(255,255,255,.34));
  backdrop-filter: blur(var(--glass-blur)) saturate(1.5); -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.5);
  border: var(--glass-border); border-radius: var(--r-lg);
  padding: 22px 24px; box-shadow: var(--glass-shadow), var(--glass-inner);
  position: relative;
}
.panel-head { display: flex; align-items: baseline; gap: 12px; margin-bottom: 16px; }
.panel-head h2 { font-size: 15.5px; font-weight: 700; }
.panel-head span { font-size: 12px; color: var(--tx-3); }

/* ---------------- KPI ---------------- */
.kpi-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 14px; margin-bottom: 24px; }
.metric {
  background: linear-gradient(155deg, rgba(255,255,255,.6), rgba(255,255,255,.36));
  backdrop-filter: blur(var(--glass-blur)) saturate(1.5); -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.5);
  border: var(--glass-border); border-radius: var(--r-lg);
  padding: 16px 18px; position: relative; overflow: hidden;
  box-shadow: var(--shadow-1);
  animation: cardIn .5s var(--ease) both;
  transition: transform .3s var(--spring), box-shadow .3s var(--ease);
}
.metric:hover { transform: translateY(-3px); box-shadow: var(--shadow-2); }
.metric:nth-child(1) { animation-delay: .02s; } .metric:nth-child(2) { animation-delay: .07s; }
.metric:nth-child(3) { animation-delay: .12s; } .metric:nth-child(4) { animation-delay: .17s; }
.metric:nth-child(5) { animation-delay: .22s; }
.metric::before {
  content: ""; position: absolute; left: 0; top: 14px; bottom: 14px; width: 3.5px; border-radius: 0 4px 4px 0;
  background: var(--m-color, var(--cy));
}
.metric .m-val { font-family: var(--font-num); font-size: 27px; font-weight: 700; color: var(--m-color, var(--cy-deep)); letter-spacing: -.5px; }
.metric .m-label { font-size: 12.5px; color: var(--tx-2); }
.metric .m-tip { font-size: 11px; color: var(--tx-3); margin-top: 7px; border-top: 1px dashed var(--line); padding-top: 7px; line-height: 1.6; }
.metric.m-live { border-color: rgba(20,168,160,.4); animation: cardIn .5s var(--ease) both, breathe 3s ease-in-out infinite 1s; }
.metric.m-live::after {
  content: "本次 +1"; position: absolute; top: 12px; right: 13px;
  font-size: 10px; font-weight: 700; color: #fff;
  background: linear-gradient(135deg, #17b3aa, #0fa39b); padding: 2px 9px; border-radius: 999px;
  animation: popIn .4s var(--spring) .5s both;
}

/* ---------------- 工作台首页 ---------------- */
.home-grid { display: grid; grid-template-columns: minmax(0, 1fr); gap: 18px; margin-bottom: 18px; }
.home-grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; }
.home-grid .panel, .home-grid-2 .panel { animation: cardIn .55s var(--ease) both; }
.home-grid .panel:nth-child(2) { animation-delay: .08s; }
.home-grid-2 .panel:nth-child(1) { animation-delay: .14s; }
.home-grid-2 .panel:nth-child(2) { animation-delay: .2s; }

#page-home .home-grid .panel {
  overflow: hidden;
  background:
    radial-gradient(420px 180px at 96% 0%, rgba(91,124,250,.09), transparent 68%),
    radial-gradient(360px 180px at 4% 100%, rgba(18,165,184,.08), transparent 70%),
    linear-gradient(155deg, rgba(255,255,255,.62), rgba(255,255,255,.34));
}
#page-home .home-grid .panel::after {
  content: "";
  position: absolute;
  width: 210px; height: 210px;
  right: -92px; bottom: -118px;
  border-radius: 46% 54% 63% 37% / 42% 45% 55% 58%;
  border: 1px solid rgba(255,255,255,.62);
  background: linear-gradient(145deg, rgba(255,255,255,.2), rgba(91,124,250,.055));
  transform: rotate(18deg);
  pointer-events: none;
}
.scenario-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 13px; position: relative; z-index: 1; }
.scenario-card {
  display: grid; grid-template-columns: 46px 1fr auto; gap: 13px; align-items: center;
  min-height: 118px;
  border: 1px solid var(--line); border-radius: var(--r-md);
  padding: 16px;
  background: linear-gradient(150deg, rgba(255,255,255,.68), rgba(255,255,255,.4));
  backdrop-filter: blur(14px) saturate(1.4); -webkit-backdrop-filter: blur(14px) saturate(1.4);
  transition: all .28s var(--spring); text-align: left;
  box-shadow: inset 0 1px 0 rgba(255,255,255,.76), 0 5px 18px rgba(43,49,64,.055);
}
.scenario-card:hover { border-color: rgba(20,168,160,.4); transform: translateY(-2px) translateX(2px); box-shadow: var(--shadow-cy); }
.scenario-card:hover .sc-go { transform: translateX(4px); color: var(--cy); }
.scenario-card .sc-icon { font-size: 27px; text-align: center; }
.scenario-card .sc-body { min-width: 0; }
.scenario-card .sc-body b { display: block; font-size: 14px; }
.scenario-card .sc-body span { font-size: 12px; color: var(--tx-2); display: block; line-height: 1.55; overflow-wrap: anywhere; }
.scenario-card .sc-body em { font-style: normal; font-size: 11.5px; color: var(--cy-deep); line-height: 1.5; display: block; margin-top: 2px; overflow-wrap: anywhere; }
.scenario-card .sc-go { color: var(--tx-3); font-size: 18px; margin-left: 4px; transition: all .25s var(--spring); }

.feed { list-style: none; display: flex; flex-direction: column; }
.feed li {
  display: grid; grid-template-columns: 30px 1fr; gap: 10px;
  padding: 11px 6px; border-bottom: 1px dashed var(--line);
  font-size: 13px;
  animation: rise .45s var(--ease) both;
}
.feed li:nth-child(1) { animation-delay: .05s; } .feed li:nth-child(2) { animation-delay: .12s; }
.feed li:nth-child(3) { animation-delay: .19s; } .feed li:nth-child(4) { animation-delay: .26s; }
.feed li:nth-child(5) { animation-delay: .33s; } .feed li:nth-child(6) { animation-delay: .4s; }
.feed li:last-child { border-bottom: none; }
.feed .f-icon { font-size: 15px; text-align: center; padding-top: 1px; }
.feed .f-sub { font-size: 11.5px; color: var(--tx-3); }
.feed .f-time { font-size: 11px; color: var(--tx-3); font-family: var(--font-num); }
.feed li.f-ok .f-icon { color: var(--cy); }
.feed li.f-conflict .f-icon { color: var(--rose); }
.feed li.f-wait .f-icon { color: var(--gold); }
.feed li.f-click { cursor: pointer; border-radius: var(--r-sm); transition: all .2s var(--ease); }
.feed li.f-click:hover { background: var(--cy-soft); transform: translateX(3px); }

.link-num {
  border: none; background: none; padding: 0 2px;
  color: var(--cy-deep); font-family: var(--font-num); font-size: 15px; font-weight: 700;
  border-bottom: 1.5px dashed rgba(20,168,160,.5); cursor: pointer;
  transition: all .2s var(--ease);
}
.link-num:hover { color: var(--cy); border-bottom-style: solid; }

.track-overview { display: flex; flex-direction: column; gap: 14px; }
.track-bar-row { display: grid; grid-template-columns: 84px 1fr 100px; gap: 12px; align-items: center; font-size: 12.5px; cursor: pointer; }
.track-bar-row:hover .tb-name { color: var(--cy-deep); }
.tb-name { color: var(--tx-2); font-weight: 600; transition: color .2s; }
.tb-bar { height: 16px; border-radius: 8px; background: rgba(38,50,80,.06); overflow: hidden; position: relative; }
.tb-bar i { position: absolute; inset: 0 auto 0 0; border-radius: 8px; opacity: .8; transform-origin: left; animation: growX .9s var(--ease) both .2s; }
.tb-meta { color: var(--tx-3); font-size: 11.5px; text-align: right; font-family: var(--font-num); }

.intake-overview { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }
.intake-cell {
  border-radius: var(--r-md); padding: 16px 12px; text-align: center;
  border: var(--glass-border);
  background: linear-gradient(155deg, rgba(255,255,255,.6), rgba(255,255,255,.38));
  backdrop-filter: blur(12px) saturate(1.4); -webkit-backdrop-filter: blur(12px) saturate(1.4);
  transition: transform .28s var(--spring), box-shadow .28s var(--ease);
}
.intake-cell:hover { transform: translateY(-3px); box-shadow: var(--shadow-1); }
.intake-cell .n { font-family: var(--font-num); font-size: 30px; font-weight: 700; }
.intake-cell .t { font-size: 12px; color: var(--tx-2); }
.intake-cell .names { font-size: 10.5px; color: var(--tx-3); margin-top: 7px; line-height: 1.55; }
.ic-open .n { color: var(--cy); } .ic-tight .n { color: var(--gold); } .ic-full .n { color: var(--tx-3); }

/* ---------------- 站位库 ---------------- */
.lib-stats { display: flex; gap: 18px; font-size: 12px; color: var(--tx-3); }
.lib-stats b { color: var(--cy-deep); font-family: var(--font-num); font-size: 15px; }
.filter-bar {
  display: flex; flex-wrap: wrap; gap: 10px 20px; align-items: center;
  background: var(--card); backdrop-filter: blur(14px);
  border: 1px solid var(--line); border-radius: var(--r-lg);
  padding: 14px 18px; margin-bottom: 14px; box-shadow: var(--shadow-1);
}
.filter-group { display: flex; align-items: center; gap: 6px; flex-wrap: wrap; }
.filter-group > span { font-size: 12px; color: var(--tx-3); }
.fchip {
  font-size: 12px; padding: 4px 13px; border-radius: 999px;
  border: 1px solid rgba(255,255,255,.7); background: linear-gradient(150deg, rgba(255,255,255,.66), rgba(255,255,255,.44)); backdrop-filter: blur(10px) saturate(1.35); -webkit-backdrop-filter: blur(10px) saturate(1.35); color: var(--tx-2);
  transition: all .22s var(--spring);
}
.fchip:hover { border-color: var(--cy); color: var(--cy-deep); transform: translateY(-1px); }
.fchip.on { background: linear-gradient(135deg, #17b8c9, #0e96ab); border-color: transparent; color: #fff; font-weight: 600; box-shadow: 0 3px 12px rgba(18,165,184,.32), inset 0 1px 0 rgba(255,255,255,.35); }
.filter-search { margin-left: auto; display: flex; gap: 8px; align-items: center; }
.filter-search input {
  background: linear-gradient(150deg, rgba(255,255,255,.66), rgba(255,255,255,.44)); backdrop-filter: blur(10px) saturate(1.35); -webkit-backdrop-filter: blur(10px) saturate(1.35); border: 1px solid rgba(255,255,255,.7); border-radius: 999px;
  padding: 7px 17px; font: inherit; font-size: 12.5px; color: var(--tx-1); width: 230px;
  transition: all .25s var(--ease);
}
.filter-search input:focus { outline: none; border-color: var(--cy); box-shadow: 0 0 0 3.5px rgba(20,168,160,.13); width: 260px; }

.sort-row { display: flex; gap: 8px; align-items: center; margin-bottom: 18px; font-size: 12.5px; color: var(--tx-3); flex-wrap: wrap; }
.sort-btn { border: none; background: none; color: var(--tx-2); font-size: 12.5px; padding: 4px 11px; border-radius: 8px; transition: all .2s var(--ease); }
.sort-btn:hover { color: var(--cy-deep); background: var(--cy-soft); }
.sort-btn.active { background: var(--cy-soft); color: var(--cy-deep); font-weight: 650; }
.sort-tip { margin-left: auto; font-family: var(--font-num); }

.lib-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(330px, 1fr)); gap: 16px; }
.lib-card {
  background: linear-gradient(160deg, rgba(255,255,255,.66), rgba(255,255,255,.4));
  backdrop-filter: blur(var(--glass-blur)) saturate(1.5); -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.5);
  border: var(--glass-border); border-radius: var(--r-lg);
  padding: 18px; cursor: pointer; position: relative;
  display: flex; flex-direction: column; gap: 12px;
  box-shadow: var(--glass-shadow), var(--glass-inner);
  animation: cardIn .5s var(--ease) both;
  transition: transform .3s var(--spring), box-shadow .3s var(--ease), border-color .3s;
}
.lib-card:nth-child(2) { animation-delay: .05s; } .lib-card:nth-child(3) { animation-delay: .1s; }
.lib-card:nth-child(4) { animation-delay: .15s; } .lib-card:nth-child(5) { animation-delay: .2s; }
.lib-card:nth-child(6) { animation-delay: .25s; } .lib-card:nth-child(n+7) { animation-delay: .3s; }
.lib-card:hover { border-color: rgba(20,168,160,.3); transform: translateY(-4px); box-shadow: var(--shadow-2); }
.lib-card::before {
  content: ""; position: absolute; top: 0; left: 18px; right: 18px; height: 3px; border-radius: 0 0 5px 5px;
  background: var(--tk-color, var(--tx-3)); opacity: .75;
}
.lc-top { display: flex; justify-content: space-between; align-items: flex-start; gap: 8px; }
.lc-name { font-size: 16px; font-weight: 700; display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
.track-tag {
  font-size: 10.5px; font-weight: 700; padding: 2.5px 10px; border-radius: 6px;
  color: #fff; background: var(--tk-color, var(--tx-3)); letter-spacing: .5px;
}
.intake { font-size: 11.5px; display: inline-flex; flex-direction: column; align-items: flex-end; gap: 1px; white-space: nowrap; }
.intake-open { color: var(--cy-deep); } .intake-tight { color: var(--gold); font-weight: 650; } .intake-full { color: var(--tx-3); font-weight: 650; }
.intake-fresh { font-style: normal; font-size: 9.5px; color: var(--tx-3); font-weight: 400; letter-spacing: .3px; }
.lc-hq { font-size: 11.5px; color: var(--tx-3); }

.lc-nums { display: grid; grid-template-columns: repeat(4, 1fr); gap: 7px; }
.lc-num { background: rgba(38,50,80,.04); border-radius: var(--r-sm); padding: 8px 4px 6px; text-align: center; transition: background .2s; }
.lib-card:hover .lc-num { background: var(--cy-soft); }
.lc-num b { font-family: var(--font-num); font-size: 14.5px; display: block; color: var(--tx-1); }
.lc-num span { font-size: 10px; color: var(--tx-3); }

.spark { height: 34px; display: flex; align-items: flex-end; gap: 2.5px; }
.spark i { flex: 1; border-radius: 3px 3px 0 0; background: linear-gradient(180deg, var(--tk-color, var(--cy)), rgba(255,255,255,0)); opacity: .55; min-height: 2px; transform-origin: bottom; animation: growY .7s var(--ease) both; transition: opacity .2s; }
.lib-card:hover .spark i { opacity: .85; }
@keyframes growY { from { transform: scaleY(0); } to { transform: scaleY(1); } }
.spark i:nth-child(2n) { animation-delay: .06s; } .spark i:nth-child(3n) { animation-delay: .12s; }
.spark-cap { font-size: 10.5px; color: var(--tx-3); margin-top: 3px; }

.cert-row { display: flex; gap: 6px; flex-wrap: wrap; align-items: center; }
.cert { font-size: 11px; padding: 3px 11px; border-radius: 999px; font-weight: 600; border: 1px solid transparent; }
.cert-on { background: var(--blue-soft); color: var(--blue); border-color: rgba(91,124,250,.3); }
.cert-off { color: var(--tx-3); border-style: dashed; border-color: var(--line-strong); opacity: .75; }
.tag-chip { font-size: 11px; padding: 3px 10px; border-radius: 999px; background: rgba(38,50,80,.05); color: var(--tx-2); }

.lc-actions { display: flex; gap: 8px; border-top: 1px dashed var(--line); padding-top: 12px; }
.lc-actions .btn { flex: 1; padding: 6.5px 8px; font-size: 12px; text-align: center; }

/* ---------------- 智能匹配 ---------------- */
.match-steps { display: flex; align-items: center; gap: 8px; font-size: 12.5px; }
.match-steps .ms { color: var(--tx-3); padding: 5px 13px; border-radius: 999px; border: 1px solid transparent; transition: all .3s var(--ease); }
.match-steps .ms.active { color: var(--cy-deep); border-color: rgba(18,165,184,.4); background: linear-gradient(150deg, rgba(255,255,255,.85), rgba(255,255,255,.6)); font-weight: 650; box-shadow: var(--glass-shadow), inset 0 1px 0 rgba(255,255,255,.8); }
.match-steps .ms.done { color: var(--tx-2); }
.match-steps i { width: 18px; height: 1px; background: var(--line-strong); }

.match-step { animation: rise .4s var(--ease) both; }

.input-hero {
  position: relative; overflow: hidden;
  background: linear-gradient(155deg, rgba(255,255,255,.55), rgba(255,255,255,.3));
  backdrop-filter: blur(var(--glass-blur)) saturate(1.6); -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.6);
  border: var(--glass-border); border-radius: 28px;
  padding: 40px 40px 30px; margin-bottom: 20px; text-align: center;
  box-shadow: var(--shadow-1);
}
.input-hero-glow {
  position: absolute; inset: -40% -10% auto; height: 220px; pointer-events: none;
  background: radial-gradient(420px 170px at 38% 0%, rgba(18,165,184,.16), transparent 70%),
              radial-gradient(360px 150px at 68% 0%, rgba(139,110,240,.12), transparent 70%);
}
.input-hero h2 { font-size: 21px; margin-bottom: 5px; position: relative; }
.input-hero > p { color: var(--tx-2); font-size: 13px; margin-bottom: 20px; position: relative; }
.input-wrap { display: flex; gap: 10px; align-items: stretch; position: relative; max-width: 760px; margin: 0 auto; }
.input-wrap textarea {
  flex: 1; background: rgba(255,255,255,.62); border: 1px solid rgba(255,255,255,.75); border-radius: 16px;
  backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px);
  padding: 14px 18px; font: inherit; font-size: 14.5px; color: var(--tx-1); resize: none;
  box-shadow: inset 0 2px 6px rgba(43,49,64,.05);
  transition: all .25s var(--ease);
}
.input-wrap textarea:focus { outline: none; border-color: var(--cy); box-shadow: 0 0 0 4px rgba(20,168,160,.12); }
.input-wrap .btn { align-self: stretch; border-radius: 16px; }
.quick-fill { display: flex; gap: 8px; justify-content: center; margin-top: 16px; flex-wrap: wrap; position: relative; }
.quick-fill button {
  font-size: 12px; padding: 6px 15px; border-radius: 999px;
  border: 1px dashed var(--line-strong); background: rgba(255,255,255,.7); color: var(--tx-2);
  transition: all .25s var(--spring);
}
.quick-fill button:hover { border-color: var(--cy); color: var(--cy-deep); border-style: solid; transform: translateY(-1.5px); box-shadow: 0 4px 12px rgba(20,168,160,.14); }

.brief-card {
  background: linear-gradient(155deg, rgba(255,255,255,.62), rgba(255,255,255,.38));
  backdrop-filter: blur(var(--glass-blur)) saturate(1.5); -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.5);
  border: 1px solid rgba(18,165,184,.4); border-radius: var(--r-lg);
  padding: 22px 24px; box-shadow: 0 10px 34px rgba(18,165,184,.14), var(--glass-inner);
  animation: rise .45s var(--ease) both;
}
.brief-head { display: flex; align-items: baseline; gap: 12px; margin-bottom: 16px; }
.brief-head h2 { font-size: 16px; }
.brief-head span { font-size: 12px; color: var(--tx-3); }
.brief-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 11px; }
.brief-field { border: 1px solid var(--line); border-radius: var(--r-md); padding: 10px 13px; background: #fbfbf9; transition: all .22s var(--ease); }
.brief-field:focus-within { border-color: var(--cy); background: rgba(255,255,255,.85); box-shadow: 0 0 0 3px rgba(18,165,184,.12); }
.brief-field label { display: block; font-size: 10.5px; color: var(--tx-3); letter-spacing: 1px; margin-bottom: 2px; }
.brief-field select, .brief-field input {
  width: 100%; border: none; background: transparent; font: inherit;
  font-size: 14px; font-weight: 600; color: var(--tx-1);
}
.brief-field select:focus, .brief-field input:focus { outline: none; color: var(--cy-deep); }
.brief-field.bf-missing { border-color: rgba(232,150,12,.5); background: #fdf6ea; }
.bf-flag { font-style: normal; color: var(--gold); font-weight: 700; margin-left: 6px; letter-spacing: .5px; }
.brief-actions { margin-top: 18px; text-align: right; }

.brief-summary-bar { display: flex; justify-content: space-between; align-items: center; gap: 12px; margin-bottom: 18px; flex-wrap: wrap; }
.brief-summary { display: flex; flex-wrap: wrap; gap: 7px; }
.brief-summary span { font-size: 12px; padding: 4px 13px; border-radius: 999px; background: linear-gradient(150deg, rgba(255,255,255,.66), rgba(255,255,255,.44)); backdrop-filter: blur(10px) saturate(1.35); -webkit-backdrop-filter: blur(10px) saturate(1.35); border: 1px solid rgba(255,255,255,.7); color: var(--tx-2); box-shadow: inset 0 1px 0 rgba(255,255,255,.75), 0 1px 3px rgba(43,49,64,.04); }
.brief-summary span b { color: var(--cy-deep); }

/* 领奖台 Top3 */
.podium { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-bottom: 22px; }
.podium-card {
  position: relative; border-radius: var(--r-lg); padding: 22px 20px 18px;
  background: linear-gradient(160deg, rgba(255,255,255,.68), rgba(255,255,255,.42));
  backdrop-filter: blur(var(--glass-blur)) saturate(1.5); -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.5);
  border: var(--glass-border);
  display: flex; flex-direction: column; gap: 11px;
  box-shadow: var(--glass-shadow), var(--glass-inner);
  animation: cardIn .55s var(--spring) both;
  transition: transform .3s var(--spring), box-shadow .3s var(--ease);
}
.podium-card:hover { transform: translateY(-4px); box-shadow: var(--shadow-2); }
.podium-card.p1 { animation-delay: .05s; border-color: rgba(20,168,160,.45); box-shadow: 0 12px 36px rgba(20,168,160,.16); }
.podium-card.p2 { animation-delay: .14s; }
.podium-card.p3 { animation-delay: .23s; }
.podium-card .p-rank {
  position: absolute; top: -13px; left: 18px;
  font-size: 11px; font-weight: 800; letter-spacing: 1px;
  padding: 4px 14px; border-radius: 999px; color: #fff;
  background: linear-gradient(135deg, #17b3aa, #0fa39b);
  box-shadow: 0 4px 12px rgba(20,168,160,.3);
  animation: popIn .45s var(--spring) .35s both;
}
.podium-card.p2 .p-rank { background: linear-gradient(135deg, #6f8bfa, #5b7cfa); box-shadow: 0 4px 12px rgba(91,124,250,.3); animation-delay: .44s; }
.podium-card.p3 .p-rank { background: #98a1b3; box-shadow: 0 4px 12px rgba(43,49,64,.16); animation-delay: .53s; }
.podium-card .p-head { display: flex; justify-content: space-between; align-items: center; gap: 8px; }
.podium-card .p-name { font-size: 17px; font-weight: 750; display: flex; gap: 8px; align-items: center; flex-wrap: wrap; }
.podium-card .p-score { font-family: var(--font-num); font-size: 32px; font-weight: 800; color: var(--cy-deep); letter-spacing: -1px; }
.podium-card.p2 .p-score { color: var(--blue); } .podium-card.p3 .p-score { color: var(--tx-2); }

.evidence {
  border-left: 3px solid var(--rose); background: var(--rose-soft);
  border-radius: 0 var(--r-md) var(--r-md) 0; padding: 10px 13px;
}
.evidence .ev-label { font-size: 10px; font-weight: 800; color: var(--rose); letter-spacing: 1.5px; margin-bottom: 2px; }
.evidence p { font-size: 13px; font-weight: 600; line-height: 1.6; }
.evidence .ev-sub { font-size: 11.5px; font-weight: 400; color: var(--tx-2); margin-top: 3px; }
.evidence.ev-weak { border-left-color: var(--line-strong); background: rgba(38,50,80,.04); }
.evidence.ev-weak .ev-label { color: var(--tx-3); }
.evidence.ev-weak p { font-weight: 400; color: var(--tx-2); font-size: 12.5px; }

.conflict-hint {
  font-size: 12px; color: var(--gold); background: var(--gold-soft);
  border: 1px dashed rgba(232,150,12,.45); border-radius: var(--r-sm); padding: 7px 11px;
}

.scorebar-wrap { display: flex; align-items: center; gap: 10px; }
.scorebar { flex: 1; height: 9px; border-radius: 5px; overflow: hidden; display: flex; background: rgba(38,50,80,.07); }
.scorebar i { height: 100%; transform-origin: left; animation: growX .8s var(--ease) both .15s; }
.sb-case { background: var(--seg-case); } .sb-cert { background: var(--seg-cert); }
.sb-cap { background: var(--seg-cap); } .sb-budget { background: var(--seg-budget); }
.score-num { font-family: var(--font-num); font-size: 15px; font-weight: 700; min-width: 30px; text-align: right; color: var(--tx-2); }

.reason-list { display: flex; flex-direction: column; gap: 5px; font-size: 12.5px; color: var(--tx-2); }
.route-tag { align-self: flex-start; font-size: 11px; font-weight: 700; padding: 3px 11px; border-radius: 6px; background: var(--violet); color: #fff; letter-spacing: 1px; }

.pick-row { display: flex; align-items: center; gap: 10px; border-top: 1px dashed var(--line); padding-top: 11px; margin-top: auto; }
.pick-row label { font-size: 12.5px; color: var(--tx-2); display: flex; align-items: center; gap: 6px; cursor: pointer; }
.pick-row input { accent-color: var(--cy); width: 15px; height: 15px; }
.pick-row .mini-actions { margin-left: auto; display: flex; gap: 6px; }
.mini-btn { font-size: 11.5px; padding: 3.5px 11px; border-radius: 8px; border: 1px solid rgba(255,255,255,.7); background: linear-gradient(150deg, rgba(255,255,255,.66), rgba(255,255,255,.44)); backdrop-filter: blur(10px) saturate(1.35); -webkit-backdrop-filter: blur(10px) saturate(1.35); color: var(--tx-2); transition: all .2s var(--spring); }
.mini-btn:hover { border-color: var(--cy); color: var(--cy-deep); transform: translateY(-1px); }

.lane-note {
  margin-bottom: 16px; padding: 11px 16px; border-radius: var(--r-md);
  background: var(--gold-soft); border: 1px solid rgba(232,150,12,.35); color: var(--gold); font-size: 13px;
}
.rest-title { font-size: 13px; color: var(--tx-3); margin: 6px 0 12px; }

.board { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 14px; }
.agent-card {
  background: linear-gradient(158deg, rgba(255,255,255,.64), rgba(255,255,255,.4)); backdrop-filter: blur(16px) saturate(1.45); -webkit-backdrop-filter: blur(16px) saturate(1.45); border: var(--glass-border); border-radius: var(--r-lg);
  padding: 16px 18px; display: flex; flex-direction: column; gap: 10px;
  box-shadow: var(--glass-shadow), var(--glass-inner);
  animation: cardIn .5s var(--ease) both;
  transition: transform .28s var(--spring), box-shadow .28s var(--ease);
}
.agent-card:hover { transform: translateY(-2.5px); box-shadow: var(--shadow-2); }
.agent-card.dead { opacity: .55; background: repeating-linear-gradient(45deg, #fff, #fff 10px, #f6f5f1 10px, #f6f5f1 20px); }
.agent-card.dead:hover { transform: none; box-shadow: var(--shadow-1); }
.ac-top { display: flex; justify-content: space-between; align-items: flex-start; gap: 8px; }
.ac-name { font-size: 14.5px; font-weight: 700; display: flex; align-items: center; gap: 8px; flex-wrap: wrap; cursor: pointer; }
.ac-name:hover { color: var(--cy-deep); }
.dead-reason { font-size: 12px; color: var(--tx-3); border: 1px dashed var(--line-strong); border-radius: var(--r-sm); padding: 6px 10px; }

.score-legend { margin-top: 18px; font-size: 12px; color: var(--tx-2); display: flex; align-items: center; gap: 10px; flex-wrap: wrap; }
.score-legend .seg { display: inline-block; width: 14px; height: 8px; border-radius: 3px; margin-right: 3px; }
.seg-case { background: var(--seg-case); } .seg-cert { background: var(--seg-cert); }
.seg-cap { background: var(--seg-cap); } .seg-budget { background: var(--seg-budget); }
.score-legend-note { color: var(--tx-3); margin-left: auto; }

.select-bar {
  position: sticky; bottom: 16px; margin-top: 20px; z-index: 20;
  display: flex; justify-content: space-between; align-items: center; gap: 14px;
  background: rgba(255,255,255,.9); border: 1px solid var(--line);
  border-radius: var(--r-lg); padding: 13px 20px;
  box-shadow: var(--shadow-2); backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
}
.select-bar > div { display: flex; gap: 8px; }
.select-bar #selectInfo { font-size: 13px; color: var(--tx-2); }

/* ---------------- 清单与路由 ---------------- */
.tabs { display: flex; gap: 4px; margin-bottom: 18px; border-bottom: 1px solid var(--line); align-items: center; }
.tab { border: none; background: none; padding: 10px 18px; font-size: 14px; font-weight: 600; color: var(--tx-3); border-bottom: 2.5px solid transparent; margin-bottom: -1px; transition: all .22s var(--ease); }
.tab:hover { color: var(--tx-1); }
.tab.active { color: var(--cy-deep); border-bottom-color: var(--cy); }

.rule-banner { margin-bottom: 16px; padding: 12px 17px; border-radius: var(--r-md); font-size: 13px; display: flex; gap: 10px; align-items: baseline; flex-wrap: wrap; animation: rise .4s var(--ease) both; }
.rule-banner.ok { background: var(--cy-soft); color: var(--cy-deep); border: 1px solid rgba(20,168,160,.3); }
.rule-banner.warn { background: var(--gold-soft); color: var(--gold); border: 1px solid rgba(232,150,12,.35); }

.dual-pane { display: grid; grid-template-columns: 1fr 46px 1fr; align-items: stretch; }
.pane { background: linear-gradient(158deg, rgba(255,255,255,.64), rgba(255,255,255,.4)); backdrop-filter: blur(16px) saturate(1.45); -webkit-backdrop-filter: blur(16px) saturate(1.45); border: var(--glass-border); border-radius: var(--r-lg); padding: 18px; box-shadow: var(--glass-shadow), var(--glass-inner); }
.pane-internal { border-top: 3px solid var(--rose); }
.pane-external { border-top: 3px solid var(--cy); }
.pane-head { display: flex; flex-direction: column; margin-bottom: 13px; }
.pane-head strong { font-size: 14.5px; }
.pane-head span { font-size: 11.5px; color: var(--tx-3); }
.pane-divider { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 8px; font-size: 10px; color: var(--tx-3); letter-spacing: 2px; }
.pane-divider span { writing-mode: vertical-lr; }
.pane-divider i { flex: 1; width: 1px; background: linear-gradient(180deg, transparent, var(--line-strong), transparent); }

.internal-list { list-style: none; display: flex; flex-direction: column; gap: 8px; }
.internal-list li {
  display: flex; align-items: center; gap: 10px; padding: 9px 13px;
  border: 1px solid var(--line); border-radius: var(--r-md); font-size: 13px; background: #fbfbf9;
  animation: rise .4s var(--ease) both;
}
.internal-list li:nth-child(-n+8) { animation-delay: calc(30ms * var(--i, 0)); }
.internal-list li.is-hl { border-color: rgba(232,96,79,.4); background: var(--rose-soft); }
.internal-list .rank { font-family: var(--font-num); font-weight: 700; color: var(--tx-3); width: 20px; }
.internal-list li.is-hl .rank { color: var(--rose); }
.internal-list .iname { font-weight: 650; flex: 1; }
.internal-list .iscore { font-family: var(--font-num); font-weight: 700; }
.internal-list .inote { font-size: 11.5px; color: var(--tx-3); }

.external-list { display: flex; flex-direction: column; gap: 13px; }
.ext-track h4 { font-size: 12.5px; color: var(--tx-2); margin-bottom: 7px; display: flex; align-items: center; gap: 6px; }
.ext-track h4::before { content: ""; width: 9px; height: 9px; border-radius: 3px; background: var(--tk-color, var(--tx-3)); }
.ext-track ul { list-style: none; display: flex; flex-direction: column; gap: 7px; }
.ext-track li { padding: 9px 13px; border: 1px solid var(--line); border-radius: var(--r-md); font-size: 12.5px; background: #fbfbf9; color: var(--tx-2); }
.ext-track li b { color: var(--tx-1); font-size: 13px; display: block; }
.ext-track li i { color: var(--tx-3); font-size: 11.5px; }
.external-foot { margin-top: 13px; font-size: 11.5px; color: var(--tx-3); border-top: 1px dashed var(--line); padding-top: 11px; line-height: 1.7; }

.handoff-bar { margin-top: 15px; display: flex; align-items: center; gap: 12px; flex-wrap: wrap; }
.handoff-bar #handoffState { font-size: 12px; color: var(--cy-deep); }

.route-toolbar { display: flex; justify-content: space-between; align-items: center; gap: 14px; margin-bottom: 15px; flex-wrap: wrap; }
.view-toggle { display: inline-flex; border: 1px solid rgba(255,255,255,.7); border-radius: 999px; overflow: hidden; background: linear-gradient(150deg, rgba(255,255,255,.66), rgba(255,255,255,.44)); backdrop-filter: blur(10px) saturate(1.35); -webkit-backdrop-filter: blur(10px) saturate(1.35); box-shadow: var(--glass-shadow); }
.view-toggle button { border: none; background: none; padding: 8px 20px; font-size: 13px; font-weight: 600; color: var(--tx-3); transition: all .25s var(--ease); }
.view-toggle button.active { background: linear-gradient(135deg, #17b3aa, #0fa39b); color: #fff; }
.route-dest { font-size: 12.5px; color: var(--tx-2); }
.route-layout { display: grid; grid-template-columns: 1fr 280px; gap: 18px; align-items: start; }
.route-main { background: linear-gradient(158deg, rgba(255,255,255,.64), rgba(255,255,255,.4)); backdrop-filter: blur(16px) saturate(1.45); -webkit-backdrop-filter: blur(16px) saturate(1.45); border: var(--glass-border); border-radius: var(--r-lg); padding: 22px; min-height: 300px; box-shadow: var(--glass-shadow), var(--glass-inner); }
.route-brief { border: 1px solid var(--line); border-radius: var(--r-md); padding: 15px 17px; background: #fbfbf9; margin-bottom: 17px; }
.route-brief h3 { font-size: 13.5px; margin-bottom: 9px; display: flex; justify-content: space-between; flex-wrap: wrap; gap: 4px; }
.route-brief h3 span { font-size: 11px; color: var(--tx-3); font-weight: 400; }
.route-brief dl { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 9px 16px; font-size: 12.5px; }
.route-brief dt { color: var(--tx-3); font-size: 11px; }
.route-brief dd { font-weight: 600; }
.route-target-card {
  display: flex; align-items: center; justify-content: space-between; gap: 10px;
  padding: 13px 17px; border-radius: var(--r-md); margin-bottom: 17px;
  background: linear-gradient(150deg, rgba(255,255,255,.7), rgba(255,255,255,.48)); backdrop-filter: blur(12px) saturate(1.4); -webkit-backdrop-filter: blur(12px) saturate(1.4); border: 1px solid rgba(255,255,255,.7); font-size: 13.5px; box-shadow: var(--glass-shadow), inset 0 1px 0 rgba(255,255,255,.75);
}
.route-target-card b { font-size: 15px; }
.status-pill { font-size: 11.5px; font-weight: 700; padding: 4px 13px; border-radius: 999px; animation: popIn .4s var(--spring) both; }
.st-wait { background: var(--gold-soft); color: var(--gold); }
.st-ok { background: var(--cy-soft); color: var(--cy-deep); }
.st-conflict { background: var(--rose-soft); color: var(--rose); }

.qu-conflict { font-size: 12.5px; border-radius: var(--r-md); padding: 10px 14px; margin-bottom: 13px; animation: rise .4s var(--ease) both; }
.qu-conflict.warn { background: var(--gold-soft); border: 1px solid rgba(232,150,12,.4); color: var(--gold); }
.qu-conflict.warn b { color: var(--gold); }
.qu-conflict.near { background: #fbfbf9; border: 1px dashed var(--line-strong); color: var(--tx-2); }
.qu-conflict.ok { background: var(--cy-soft); border: 1px solid rgba(20,168,160,.28); color: var(--cy-deep); }

.qu-reasons { margin-bottom: 17px; }
.qu-reasons h4 { font-size: 12.5px; color: var(--tx-2); margin-bottom: 7px; }
.qu-reasons ul { list-style: none; display: flex; flex-direction: column; gap: 5px; font-size: 12.5px; color: var(--tx-2); }
.qu-reasons li::before { content: "· "; color: var(--rose); font-weight: 700; }
.route-actions { display: flex; gap: 10px; flex-wrap: wrap; }
.decision-note { margin-top: 15px; padding: 13px 15px; border-radius: var(--r-md); font-size: 13px; border: 1px solid var(--line); background: #fbfbf9; animation: rise .4s var(--ease) both; }
.decision-note.ok { background: var(--cy-soft); border-color: rgba(20,168,160,.3); }
.decision-note.conflict { background: var(--rose-soft); border-color: rgba(232,96,79,.3); }
.decision-note b { display: block; margin-bottom: 3px; }
.alt-suggest { margin-top: 11px; padding: 11px 15px; border-radius: var(--r-md); background: rgba(255,255,255,.6); backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px); border: 1.5px dashed rgba(232,96,79,.5); font-size: 13px; animation: rise .45s var(--ease) .12s both; }
.alt-suggest .btn { margin-top: 9px; }

.route-timeline { background: var(--card); backdrop-filter: blur(14px); border: 1px solid var(--line); border-radius: var(--r-lg); padding: 18px; position: sticky; top: 22px; box-shadow: var(--shadow-1); }
.route-timeline h3 { font-size: 13px; margin-bottom: 11px; color: var(--tx-2); }
.route-timeline ul { list-style: none; }
.route-timeline li { position: relative; padding: 0 0 15px 19px; font-size: 12.5px; color: var(--tx-2); animation: rise .35s var(--ease) both; }
.route-timeline li::before { content: ""; position: absolute; left: 0; top: 5px; width: 9px; height: 9px; border-radius: 50%; background: var(--line-strong); transition: all .3s; }
.route-timeline li.tl-now::before { background: var(--cy); box-shadow: 0 0 0 4px rgba(20,168,160,.15); }
.route-timeline li.tl-done::before { background: var(--blue); }
.route-timeline li:not(:last-child)::after { content: ""; position: absolute; left: 4px; top: 16px; bottom: 3px; width: 1px; background: var(--line); }
.route-timeline li b { display: block; color: var(--tx-1); }
.route-timeline li time { font-size: 11px; color: var(--tx-3); font-family: var(--font-num); }
.tl-data-tag { display: inline-block; margin-top: 3px; font-size: 10.5px; font-weight: 700; background: var(--cy-soft); color: var(--cy-deep); border-radius: 5px; padding: 1.5px 8px; }

/* ---------------- 对比 ---------------- */
.compare-picker { display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 20px; }
.cp-chip {
  font-size: 12.5px; padding: 5.5px 15px; border-radius: 999px;
  border: 1px solid rgba(255,255,255,.7); background: linear-gradient(150deg, rgba(255,255,255,.66), rgba(255,255,255,.44)); backdrop-filter: blur(10px) saturate(1.35); -webkit-backdrop-filter: blur(10px) saturate(1.35); color: var(--tx-2);
  transition: all .22s var(--spring); box-shadow: 0 1px 2px rgba(43,49,64,.03);
}
.cp-chip:hover { border-color: var(--cy); color: var(--cy-deep); transform: translateY(-1.5px); }
.cp-chip.on { background: var(--cy); border-color: var(--cy); color: #fff; font-weight: 650; box-shadow: 0 4px 12px rgba(20,168,160,.28); }
.compare-empty { text-align: center; padding: 80px 0; color: var(--tx-3); }
.compare-empty .ce-icon { font-size: 46px; margin-bottom: 12px; opacity: .35; }
.compare-empty span { font-size: 12px; }
.compare-area { display: flex; flex-direction: column; gap: 18px; }
.compare-area > * { animation: cardIn .5s var(--ease) both; }
.compare-area > *:nth-child(2) { animation-delay: .08s; }
.compare-radar-row { display: flex; gap: 18px; flex-wrap: wrap; }
.radar-panel { width: 400px; flex: none; }
.radar-legend { display: flex; gap: 14px; justify-content: center; font-size: 12.5px; margin-top: 8px; flex-wrap: wrap; }
.radar-legend span { display: inline-flex; align-items: center; gap: 5px; }
.radar-legend i { width: 13px; height: 3.5px; border-radius: 2px; display: inline-block; }
.compare-table-wrap { overflow-x: auto; }
.compare-table { width: 100%; border-collapse: collapse; font-size: 13px; }
.compare-table th, .compare-table td { padding: 9px 13px; text-align: left; border-bottom: 1px solid var(--line); }
.compare-table th { color: var(--tx-3); font-size: 11.5px; font-weight: 600; letter-spacing: .5px; }
.compare-table td:first-child { color: var(--tx-3); font-size: 12px; white-space: nowrap; }
.compare-table td b { font-family: var(--font-num); font-size: 14px; }
.compare-table td.best { background: var(--cy-soft); border-radius: 6px; }
.compare-table td.best b { color: var(--cy-deep); }
.compare-table td.best::after { content: "▲"; font-size: 9px; color: var(--cy); margin-left: 4px; }
.compare-cases { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 14px; }
.cc-col h4 { font-size: 13.5px; margin-bottom: 9px; }
.cc-col .case-item { border: 1px solid var(--line); background: #fbfbf9; border-radius: var(--r-md); padding: 10px 13px; font-size: 12px; color: var(--tx-2); margin-bottom: 8px; transition: all .2s var(--ease); }
.cc-col .case-item:hover { background: rgba(255,255,255,.75); box-shadow: var(--glass-shadow); }
.cc-col .case-item b { color: var(--tx-1); display: block; font-size: 12.5px; }
.cc-col .case-item .ci-meta { color: var(--tx-3); font-size: 11px; }

.compare-exit .exit-bar {
  display: flex; align-items: center; gap: 10px; flex-wrap: wrap;
  background: var(--card); backdrop-filter: blur(14px); border: 1px solid var(--line); border-radius: var(--r-lg);
  padding: 15px 20px; box-shadow: var(--shadow-1);
}
.compare-exit .exit-bar span { font-size: 13px; color: var(--tx-2); }

/* ---------------- 洞察 ---------------- */
.insight-grid { display: grid; grid-template-columns: 460px 1fr; gap: 18px; margin-bottom: 18px; }
.insight-side { display: flex; flex-direction: column; gap: 18px; }
#sessionLog { list-style: none; display: flex; flex-direction: column; gap: 8px; }
#sessionLog li { font-size: 12.5px; padding: 10px 14px; border-radius: var(--r-md); background: var(--cy-soft); border: 1px solid rgba(20,168,160,.25); animation: rise .4s var(--ease) both; }
#sessionLog li.empty { background: #fbfbf9; border-style: dashed; border-color: var(--line-strong); color: var(--tx-3); }
#sessionLog li b { color: var(--cy-deep); }
.roadmap-steps { display: flex; flex-direction: column; gap: 11px; }
.roadmap-steps > div { display: flex; gap: 12px; align-items: baseline; font-size: 13px; padding-left: 11px; border-left: 3px solid var(--line-strong); }
.roadmap-steps > div:first-child { border-left-color: var(--cy); }
.roadmap-steps b { white-space: nowrap; }
.roadmap-steps span { color: var(--tx-2); font-size: 12.5px; }

.fw-node { fill: #fff; stroke: var(--line-strong); stroke-width: 1.2; }
.fw-node-hl { fill: rgba(20,168,160,.09); stroke: var(--cy); stroke-width: 1.5; }
.fw-label { font-size: 13px; font-weight: 650; fill: var(--tx-1); }
.fw-sub { font-size: 10px; fill: var(--tx-2); }
.fw-arrow { stroke: var(--tx-3); stroke-width: 1.5; fill: none; marker-end: url(#arrowhead); }
.fw-center-t { font-size: 15px; font-weight: 700; fill: var(--cy-deep); }
.fw-center-s { font-size: 10.5px; fill: var(--tx-2); }

.trend-chips { display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 14px; }
#trendSvg { width: 100%; height: auto; }
.trend-axis { stroke: var(--line-strong); stroke-width: 1; }
.trend-grid-l { stroke: rgba(38,50,80,.1); stroke-width: .5; stroke-dasharray: 3 4; }
.trend-line { fill: none; stroke-width: 2.5; stroke-linecap: round; }
.trend-area { opacity: .1; }
.trend-txt { font-size: 10.5px; fill: var(--tx-3); font-family: var(--font-num); }

/* ---------------- 详情抽屉 ---------------- */
.drawer-mask { position: fixed; inset: 0; background: rgba(43,49,64,.2); backdrop-filter: blur(8px) saturate(1.2); -webkit-backdrop-filter: blur(8px) saturate(1.2); z-index: 90; animation: fadeIn .25s ease both; }
@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
.drawer {
  position: fixed; top: 10px; right: 10px; bottom: 10px; width: min(560px, 94vw); z-index: 95;
  background: linear-gradient(165deg, rgba(255,255,255,.82), rgba(255,255,255,.66));
  backdrop-filter: blur(28px) saturate(1.6); -webkit-backdrop-filter: blur(28px) saturate(1.6);
  border: var(--glass-border);
  border-radius: var(--r-lg);
  box-shadow: -24px 0 70px rgba(43,49,64,.22);
  animation: slideIn .32s var(--spring) both;
  overflow-y: auto;
}
@keyframes slideIn { from { transform: translateX(60px); opacity: 0; } to { transform: none; opacity: 1; } }
.drawer-inner { padding: 28px 30px 44px; }
.dw-close { position: absolute; top: 18px; right: 20px; background: rgba(255,255,255,.7); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); border: 1px solid rgba(255,255,255,.8); border-radius: 50%; width: 34px; height: 34px; color: var(--tx-2); font-size: 15px; transition: all .22s var(--spring); }
.dw-close:hover { color: var(--rose); border-color: var(--rose); transform: rotate(90deg); }
.dw-head { margin-bottom: 18px; padding-right: 44px; }
.dw-name { font-size: 22px; font-weight: 800; display: flex; align-items: center; gap: 10px; flex-wrap: wrap; }
.dw-sub { font-size: 12.5px; color: var(--tx-3); margin-top: 5px; line-height: 1.7; }
.dw-kpis { display: grid; grid-template-columns: repeat(4, 1fr); gap: 9px; margin-bottom: 17px; }
.dw-kpi { background: linear-gradient(150deg, rgba(255,255,255,.6), rgba(255,255,255,.38)); border: 1px solid rgba(255,255,255,.65); border-radius: var(--r-md); padding: 11px 6px; text-align: center; box-shadow: inset 0 1px 0 rgba(255,255,255,.7); }
.dw-kpi b { font-family: var(--font-num); font-size: 17px; display: block; color: var(--cy-deep); }
.dw-kpi span { font-size: 10.5px; color: var(--tx-3); }
.dw-section { margin-bottom: 19px; }
.dw-section h3 { font-size: 13.5px; margin-bottom: 10px; color: var(--tx-2); display: flex; align-items: center; gap: 8px; }
.dw-section h3::after { content: ""; flex: 1; height: 1px; background: var(--line); }
.dw-radar-wrap { display: flex; gap: 10px; align-items: center; }
.dw-radar-wrap svg { width: 210px; flex: none; }
.dw-cap-list { flex: 1; display: grid; grid-template-columns: 1fr 1fr; gap: 7px 12px; font-size: 12px; color: var(--tx-2); }
.dw-cap-list b { font-family: var(--font-num); }
.grade-A { color: var(--cy-deep); } .grade-B { color: var(--blue); } .grade-C { color: var(--gold); } .grade-D { color: var(--tx-3); }
.dw-case { border: 1px solid var(--line); border-left: 3px solid var(--rose); background: #fbfbf9; border-radius: 0 var(--r-md) var(--r-md) 0; padding: 11px 14px; margin-bottom: 8px; font-size: 12.5px; }
.dw-case b { display: block; font-size: 13px; }
.dw-case .dc-meta { color: var(--tx-3); font-size: 11px; margin-top: 2px; }
.dw-brands { display: flex; gap: 6px; flex-wrap: wrap; }
.dw-actions { display: flex; gap: 10px; margin-top: 22px; }
.dw-actions .btn { flex: 1; text-align: center; }

/* 抽屉：当前商机适配试算 */
.fit-block { border-radius: var(--r-md); padding: 13px 16px; margin-bottom: 17px; border: 1px solid var(--line-strong); background: #fbfbf9; animation: rise .4s var(--ease) .1s both; }
.fit-block .fit-head { font-size: 11px; font-weight: 800; letter-spacing: 1px; color: var(--tx-3); margin-bottom: 8px; }
.fit-block .fit-head span { font-weight: 400; letter-spacing: 0; margin-left: 6px; }
.fit-block p { font-size: 12.5px; color: var(--tx-2); margin-top: 7px; }
.fit-good { border-color: rgba(20,168,160,.4); background: var(--cy-soft); }
.fit-good .fit-head { color: var(--cy-deep); }
.fit-mid { border-color: rgba(232,150,12,.35); }
.fit-mid .fit-head { color: var(--gold); }
.fit-bad { border-color: rgba(232,96,79,.35); background: var(--rose-soft); }
.fit-bad .fit-head { color: var(--rose); }
.fit-score { display: flex; align-items: center; gap: 10px; }
.fit-score b { font-family: var(--font-num); font-size: 27px; color: var(--tx-1); }
.fit-score i { font-style: normal; font-size: 10.5px; color: var(--tx-3); }
.fit-score .scorebar { flex: 1; }
.fit-warn { color: var(--gold) !important; font-weight: 600; }

/* ---------------- 台词 / 其他 ---------------- */

.toast-wrap { position: fixed; bottom: 26px; left: 50%; transform: translateX(-50%); z-index: 120; display: flex; flex-direction: column; gap: 8px; align-items: center; }
.toast {
  background: rgba(43,49,64,.78); color: #fff; padding: 11px 24px; border-radius: 999px; font-size: 13px;
  backdrop-filter: blur(16px) saturate(1.4); -webkit-backdrop-filter: blur(16px) saturate(1.4);
  border: 1px solid rgba(255,255,255,.18);
  box-shadow: 0 10px 32px rgba(43,49,64,.3), inset 0 1px 0 rgba(255,255,255,.15);
  animation: toastIn .35s var(--spring) both;
}
@keyframes toastIn { from { opacity: 0; transform: translateY(14px) scale(.94); } to { opacity: 1; transform: none; } }

:focus-visible { outline: 2px solid var(--cy); outline-offset: 2px; }

/* ---------------- 首页 Hero：单一主线 ---------------- */
.hero {
  text-align: center; padding: 46px 30px 34px; margin-bottom: 22px;
  background: linear-gradient(160deg, rgba(255,255,255,.6), rgba(255,255,255,.32));
  backdrop-filter: blur(var(--glass-blur)) saturate(1.6); -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.6);
  border: var(--glass-border); border-radius: 28px;
  box-shadow: var(--glass-shadow), var(--glass-inner);
  position: relative; overflow: hidden;
  animation: rise .5s var(--ease) both;
}
.hero::before {
  content: ""; position: absolute; top: -60%; left: -20%; width: 140%; height: 120%;
  background: radial-gradient(600px 200px at 30% 10%, rgba(18,165,184,.14), transparent 65%),
              radial-gradient(500px 180px at 72% 5%, rgba(139,110,240,.12), transparent 65%),
              radial-gradient(400px 160px at 52% 8%, rgba(255,140,190,.08), transparent 60%);
  pointer-events: none;
  animation: heroShimmer 12s ease-in-out infinite alternate;
}
@keyframes heroShimmer { from { transform: translateX(-3%) scale(1); opacity: .9; } to { transform: translateX(3%) scale(1.06); opacity: 1; } }
.hero > * { position: relative; }
.hero-kicker { font-size: 12px; letter-spacing: 3px; color: var(--cy-deep); font-weight: 700; margin-bottom: 10px; }
.hero h1 { font-size: 32px; font-weight: 750; letter-spacing: .5px; margin-bottom: 8px; }
.hero-sub { color: var(--tx-2); font-size: 14px; margin-bottom: 26px; }
.hero-input { display: flex; gap: 10px; max-width: 640px; margin: 0 auto; }
.hero-input input {
  flex: 1; background: rgba(255,255,255,.6); border: 1px solid rgba(255,255,255,.8); border-radius: 16px;
  backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);
  padding: 14px 20px; font: inherit; font-size: 14.5px; color: var(--tx-1);
  transition: all .25s var(--ease); box-shadow: inset 0 2px 6px rgba(43,49,64,.05), 0 2px 8px rgba(43,49,64,.04);
}
.hero-input input:focus { outline: none; border-color: var(--cy); box-shadow: 0 0 0 4px rgba(20,168,160,.12); }
.hero-divider { display: flex; align-items: center; gap: 14px; max-width: 640px; margin: 24px auto 18px; color: var(--tx-3); font-size: 12px; }
.hero-divider::before, .hero-divider::after { content: ""; flex: 1; height: 1px; background: var(--line-strong); }
.hero .ql-industries { justify-content: center; }

/* 首页右上角：个人介绍入口 */
.intro-bubble {
  position: absolute; top: 18px; right: 20px; z-index: 3;
  display: inline-flex; align-items: center; gap: 8px;
  padding: 8px 12px 8px 9px; border-radius: 999px;
  border: 1px solid rgba(255,255,255,.82);
  background: linear-gradient(145deg, rgba(255,255,255,.82), rgba(255,255,255,.56));
  color: var(--tx-1); text-decoration: none; font-size: 12.5px; font-weight: 700;
  box-shadow: 0 8px 24px rgba(43,49,64,.10), inset 0 1px 0 rgba(255,255,255,.9);
  backdrop-filter: blur(14px) saturate(1.4); -webkit-backdrop-filter: blur(14px) saturate(1.4);
  transition: transform .24s var(--spring), box-shadow .24s var(--ease), border-color .24s var(--ease);
}
.intro-bubble::after { content: "↗"; color: var(--cy-deep); font-size: 13px; transition: transform .24s var(--spring); }
.intro-bubble:hover {
  transform: translateY(-2px); border-color: rgba(20,168,160,.38);
  box-shadow: 0 12px 30px rgba(43,49,64,.14), inset 0 1px 0 rgba(255,255,255,.95);
}
.intro-bubble:hover::after { transform: translate(2px,-2px); }
.intro-bubble-avatar {
  width: 25px; height: 25px; border-radius: 50%; display: inline-flex; align-items: center; justify-content: center;
  background: linear-gradient(135deg, var(--cy), #76c9d3); color: #fff; font-size: 11px; font-weight: 800;
  box-shadow: 0 3px 10px rgba(20,168,160,.24);
}
@media (max-width: 760px) {
  .intro-bubble { top: 12px; right: 12px; padding: 7px 10px 7px 7px; font-size: 11.5px; }
  .intro-bubble-avatar { width: 22px; height: 22px; font-size: 10px; }
  .hero { padding-top: 58px; }
}

/* 推荐规则说明行（内部规则，不开放调节） */
.rule-line {
  font-size: 12.5px; color: var(--tx-2); line-height: 1.7;
  background: linear-gradient(150deg, rgba(255,255,255,.55), rgba(255,255,255,.34));
  backdrop-filter: blur(14px) saturate(1.4); -webkit-backdrop-filter: blur(14px) saturate(1.4);
  border: var(--glass-border); border-radius: var(--r-md);
  padding: 10px 16px; margin-bottom: 18px;
  box-shadow: var(--glass-inner);
}
.rule-line b { color: var(--rose); }

/* ---------------- 行业速查 ---------------- */
.quick-lookup { margin-bottom: 20px; animation: cardIn .5s var(--ease) both; }
.ql-industries { display: flex; gap: 9px; flex-wrap: wrap; }
.ql-chip {
  display: inline-flex; align-items: center; gap: 7px;
  font-size: 13.5px; font-weight: 600; padding: 9px 18px; border-radius: 14px;
  border: 1px solid rgba(255,255,255,.7);
  background: linear-gradient(150deg, rgba(255,255,255,.7), rgba(255,255,255,.45));
  backdrop-filter: blur(10px) saturate(1.4); -webkit-backdrop-filter: blur(10px) saturate(1.4);
  color: var(--tx-1);
  transition: all .25s var(--spring); box-shadow: inset 0 1px 0 rgba(255,255,255,.8), 0 1px 3px rgba(43,49,64,.05);
}
.ql-chip b { font-size: 10.5px; font-weight: 700; color: var(--tx-3); background: rgba(38,50,80,.06); border-radius: 999px; padding: 1px 8px; }
.ql-chip:hover:not(:disabled) { border-color: var(--cy); color: var(--cy-deep); transform: translateY(-2px); box-shadow: 0 5px 14px rgba(20,168,160,.16); }
.ql-chip.on { background: linear-gradient(135deg, #17b8c9, #0e96ab 60%, #5b7cfa 150%); border-color: transparent; color: #fff; box-shadow: var(--shadow-cy), inset 0 1px 0 rgba(255,255,255,.4); }
.ql-chip.on b { background: rgba(255,255,255,.25); color: #fff; }
.ql-chip:disabled { opacity: .4; cursor: not-allowed; }
.ql-result { margin-top: 16px; animation: rise .4s var(--ease) both; }
.ql-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(230px, 1fr)); gap: 12px; }
.ql-track { border: 1px solid rgba(255,255,255,.6); border-radius: var(--r-md); padding: 12px 13px; background: rgba(255,255,255,.4); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); }
.ql-track h4 { font-size: 12.5px; margin-bottom: 8px; display: flex; align-items: center; gap: 6px; color: var(--tx-2); }
.ql-track h4::before { content: ""; width: 9px; height: 9px; border-radius: 3px; background: var(--tk-color); }
.ql-track h4 span { font-size: 11px; color: var(--tx-3); font-weight: 400; }
.ql-agent {
  display: flex; align-items: center; gap: 8px; width: 100%; text-align: left;
  border: 1px solid rgba(255,255,255,.65); background: linear-gradient(150deg, rgba(255,255,255,.66), rgba(255,255,255,.44)); backdrop-filter: blur(10px) saturate(1.35); -webkit-backdrop-filter: blur(10px) saturate(1.35); border-radius: var(--r-sm);
  padding: 7px 10px; margin-bottom: 6px; transition: all .2s var(--spring);
}
.ql-agent:hover { border-color: var(--cy); transform: translateX(2px); box-shadow: 0 3px 10px rgba(20,168,160,.12); }
.ql-agent .qa-name { font-size: 13px; font-weight: 650; }
.ql-agent .qa-meta { font-size: 10.5px; color: var(--tx-3); flex: 1; }
.ql-agent .intake { font-size: 10px; }
.ql-actions { display: flex; gap: 10px; margin-top: 14px; flex-wrap: wrap; }

/* 应答话术卡 */
.answer-card {
  margin-top: 14px; border: 1px solid rgba(20,168,160,.3); border-radius: var(--r-md);
  background: linear-gradient(150deg, rgba(255,255,255,.66), rgba(255,255,255,.44)); backdrop-filter: blur(10px) saturate(1.35); -webkit-backdrop-filter: blur(10px) saturate(1.35); overflow: hidden; animation: rise .4s var(--ease) both;
  box-shadow: 0 8px 26px rgba(20,168,160,.1);
}
.answer-card .ac-head { display: flex; align-items: center; gap: 10px; padding: 10px 16px; border-bottom: 1px dashed var(--line); }
.answer-card .ac-head b { font-size: 13px; }
.answer-card .ac-head span { font-size: 11px; color: var(--tx-3); flex: 1; }
.answer-card pre {
  font-family: inherit; font-size: 12.5px; line-height: 1.8; color: var(--tx-1);
  padding: 14px 18px; white-space: pre-wrap; word-break: break-word; background: #fbfbf9;
}

@media (max-width: 1260px) {
  .scenario-cards { grid-template-columns: 1fr; }
  .scenario-card { min-height: 0; }
}

@media (max-width: 1100px) {
  body { grid-template-columns: 1fr; }
  .sidenav { position: static; height: auto; flex-direction: row; align-items: center; flex-wrap: wrap; }
  .nav-list { flex-direction: row; flex-wrap: wrap; }
  .nav-list button em { display: none; }
  .nav-foot { flex-direction: row; align-items: center; margin: 0; }
  .home-grid, .home-grid-2, .insight-grid { grid-template-columns: 1fr; }
  .podium { grid-template-columns: 1fr; }
  .route-layout { grid-template-columns: 1fr; }
  .radar-panel { width: 100%; }
}
@media (max-width: 760px) {
  .main { padding: 20px 16px 60px; }
  .dual-pane { grid-template-columns: 1fr; gap: 12px; }
  .pane-divider { display: none; }
  .dw-kpis { grid-template-columns: repeat(2, 1fr); }
}

/* ===== 服务线与资源配置（v6） ===== */
.dw-res { font-size: 12.5px; color: var(--tx-2); margin-top: 8px; line-height: 1.55; padding: 8px 10px; background: var(--bg-2, #f6f8f7); border-radius: 8px; }
.dw-res b { display: inline-block; margin-right: 8px; color: var(--tx-1); font-size: 12px; }
.ext-res { display: block; font-size: 11.5px; color: var(--tx-3); margin-top: 3px; }

/* ===== 首屏"为什么做这个"断点区（v6） ===== */
.why-strip { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin: 26px auto 0; max-width: 860px; }
.why-item {
  background: linear-gradient(155deg, rgba(255,255,255,.55), rgba(255,255,255,.32));
  backdrop-filter: blur(14px) saturate(1.4); -webkit-backdrop-filter: blur(14px) saturate(1.4);
  border: var(--glass-border); border-radius: 16px; padding: 14px 16px; text-align: left;
  box-shadow: var(--glass-inner);
  transition: transform .28s var(--spring), box-shadow .28s var(--ease);
}
.why-item:hover { transform: translateY(-3px); box-shadow: var(--glass-shadow), var(--glass-inner); }
.why-item b { display: block; font-size: 13px; margin-bottom: 4px; color: var(--tx-1); }
.why-item span { font-size: 12.5px; color: var(--tx-2); line-height: 1.5; }
.why-kicker { text-align: center; font-size: 12px; letter-spacing: 2px; color: var(--tx-3); margin-top: 30px; }
@media (max-width: 760px) { .why-strip { grid-template-columns: 1fr; } }

/* ===== v9 案例类型标签 ===== */
.case-type-tag {
  display: inline-block; font-size: 10px; font-weight: 700;
  padding: 1px 7px; border-radius: 4px; letter-spacing: .5px;
  background: var(--violet); color: #fff;
}
.case-type-tag:nth-child(n) { opacity: .85; }
.dc-meta .case-type-tag { margin-left: 4px; }
.ci-meta .case-type-tag { margin-left: 2px; }

/* ===== v9 rule-banner info variant ===== */
.rule-banner.info { background: var(--cy-soft); color: var(--cy-deep); border: 1px solid rgba(20,168,160,.3); }

/* ===== v9：口径清楚 · 案例优先 · 全字段检索 ===== */
.hero-scope { display:flex; justify-content:center; gap:8px; flex-wrap:wrap; margin:-12px auto 22px; }
.hero-scope span { font-size:11px; color:var(--tx-2); padding:4px 11px; border-radius:999px; background:rgba(255,255,255,.5); border:1px solid rgba(255,255,255,.72); }
.data-scope-bar { display:flex; align-items:center; justify-content:space-between; gap:18px; margin-bottom:14px; padding:13px 17px; border:1px solid rgba(18,165,184,.18); border-radius:var(--r-lg); background:linear-gradient(145deg,rgba(237,252,250,.78),rgba(255,255,255,.52)); box-shadow:var(--glass-shadow); }
.scope-main { min-width:225px; display:flex; flex-direction:column; gap:3px; }
.scope-main b { font-size:13px; color:var(--cy-deep); }
.scope-main span { font-size:11px; color:var(--tx-2); line-height:1.45; }
.scope-items { display:flex; justify-content:flex-end; gap:9px; flex-wrap:wrap; }
.scope-items span { font-size:10.5px; color:var(--tx-2); background:rgba(255,255,255,.58); padding:5px 9px; border-radius:8px; border:1px solid rgba(255,255,255,.72); }
.scope-items i { font-style:normal; color:var(--cy-deep); font-weight:700; margin-right:4px; }
.v9-card { gap:11px; }
.lc-evidence { border:1px solid rgba(240,121,106,.2); border-left:3px solid var(--rose); border-radius:0 var(--r-md) var(--r-md) 0; padding:10px 12px; background:linear-gradient(145deg,rgba(255,247,245,.82),rgba(255,255,255,.5)); }
.lce-head { display:flex; justify-content:space-between; gap:8px; margin-bottom:5px; }
.lce-head span { color:var(--rose); font-size:10.5px; font-weight:700; letter-spacing:.4px; }
.lce-head em { font-style:normal; color:var(--tx-3); font-size:10px; }
.lc-evidence > b { display:block; font-size:13px; margin-bottom:2px; }
.lc-evidence > p { font-size:11.5px; color:var(--tx-2); line-height:1.5; min-height:34px; }
.lce-meta { display:flex; align-items:center; flex-wrap:wrap; gap:5px; margin-top:7px; }
.lce-meta > span:not(.case-type-tag) { font-size:10px; color:var(--tx-3); padding:2px 6px; background:rgba(38,50,80,.04); border-radius:5px; }
.lc-capability { display:grid; grid-template-columns:116px 1fr; align-items:center; gap:8px; padding:7px 9px; border-radius:var(--r-md); background:rgba(38,50,80,.028); }
.lc-capability svg { width:112px; height:104px; overflow:visible; }
.lc-cap-summary { display:flex; flex-direction:column; gap:4px; min-width:0; }
.lc-cap-summary > b { font-size:12.5px; }
.lc-cap-summary > span { font-size:10.5px; color:var(--tx-3); line-height:1.4; }
.lc-cap-summary > div { display:flex; flex-wrap:wrap; gap:4px; margin-top:3px; }
.lc-cap-summary i { font-style:normal; font-size:10px; color:var(--cy-deep); padding:2px 6px; border-radius:5px; background:var(--cy-soft); }
.lc-position { display:grid; grid-template-columns:1fr 1.45fr; gap:7px; }
.lc-position span { padding:7px 9px; border-radius:var(--r-sm); background:rgba(38,50,80,.04); min-width:0; }
.lc-position i { display:block; font-style:normal; color:var(--tx-3); font-size:9.5px; margin-bottom:2px; }
.lc-position b { display:block; font-size:11px; color:var(--tx-1); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
@media (max-width: 920px) {
  .data-scope-bar { align-items:flex-start; flex-direction:column; }
  .scope-items { justify-content:flex-start; }
  .filter-search { width:100%; margin-left:0; }
  .filter-search input { flex:1; width:auto; }
  .filter-search input:focus { width:auto; }
}
@media (max-width: 560px) {
  .hero-scope { display:none; }
  .scope-items span { width:100%; }
  .lib-grid { grid-template-columns:1fr; }
  .lc-capability { grid-template-columns:100px 1fr; }
  .lc-capability svg { width:96px; height:94px; }
  .lc-position { grid-template-columns:1fr; }
}
</style>
<script src="https://fe-static.xhscdn.com/data/cowork-uba-script/dist/uba-script.js?random=1789807791317299"></script></head>
<body>

<!-- ======== 左侧导航 ======== -->
<aside class="sidenav">
  <div class="logo">
    <div class="logo-mark">直</div>
    <div class="logo-text"><b>直渠握手</b><span>优质代理推荐工作台</span></div>
  </div>
  <nav class="nav-list" id="navList">
    <button data-page="home" class="active"><i>◈</i>需求工作台<em>记录需求 · 快速判断</em></button>
    <button data-page="library"><i>▤</i>优质代理站位库<em>资质 · 行业 · 案例</em></button>
    <button data-page="match"><i>⌖</i>匹配参考<em>类似案例 · 适配依据</em></button>
    <button data-page="compare"><i>⇄</i>代理对比<em>能力 · 案例差异</em></button>
    <button data-page="insights"><i>◉</i>协同记录<em>确认结果 · 承接反馈</em></button>
  </nav>
  <div class="nav-foot">
    <div class="compliance-mini">
      <b>平台真实流程</b>
      需求Brief → 优质代理站位 → 证据匹配 → 内部比选清单 → 路由渠道确认<br/>对客输出转商服按标准流程补齐复核
    </div>
    <span class="author-badge">署名：林昕悦（实习）</span>
  </div>
</aside>

<!-- ======== 主区域 ======== -->
<main class="main" id="main">

  <!-- ════════ 工作台首页 ════════ -->
  <section class="page" id="page-home">
    <div class="hero">
      <a class="intro-bubble" href="https://picasso-private-1251524319.cos.ap-shanghai.myqcloud.com/formula-static/dibp/20260820-061713-b89b57/index.html" target="_blank" rel="noopener noreferrer" aria-label="在新窗口打开林昕悦的介绍">
        <span class="intro-bubble-avatar">林</span><span>林昕悦的介绍</span>
      </a>
      <p class="hero-kicker">直渠握手优质代理推荐工作台</p>
      <h1>为客户找到对的代理</h1>
      <p class="hero-sub">一句话描述客户需求进入匹配参考，或按行业直接速查优质代理站位</p>
      <div class="hero-scope"><span>代理池：优质上榜综合代理</span><span>体量：仅展示行业相对站位</span><span>结论：渠道终审后对客</span></div>
      <div class="hero-input">
        <input id="heroInput" placeholder="例：新锐辅食品牌想做小红书冷启动，月预算 30 万…"/>
        <button class="btn btn-primary btn-lg" id="heroGo">开始匹配</button>
      </div>
      <div class="hero-divider"><span>或按行业速查</span></div>
      <div class="ql-industries" id="qlIndustries"></div>
      <p class="why-kicker">为什么需要这个工具</p>
      <div class="why-strip">
        <div class="why-item"><b>能力数据没进匹配</b><span>优质代理的认证与考核数据只用于内部管理，从未进入商机撮合——推荐环节靠个人经验和记忆。</span></div>
        <div class="why-item"><b>直客拿到的是不排序的名单</b><span>直客需要快速了解优质代理的站位，但现有流程给出的是一份无排序、无证据的名单。</span></div>
        <div class="why-item"><b>推荐缺少证据支撑</b><span>对客推荐应基于案例证据和能力的结构化匹配，而非主观印象——这里让推荐有据可查。</span></div>
      </div>
    </div>

    <div class="ql-result" id="qlResult" hidden=""></div>

    <div class="home-grid">
      <div class="panel">
        <div class="panel-head"><h2>演示场景</h2><span>三条完整业务动线，点击直达</span></div>
        <div class="scenario-cards" id="homeScenarios"></div>
      </div>
    </div>
  </section>

  <!-- ════════ 优质代理站位库 ════════ -->
  <section class="page" id="page-library" hidden="">
    <header class="page-head">
      <div><h1>优质代理站位库</h1><p>优质上榜代理 · 综合类型（4A/本土）· 行业 × 能力 × 案例</p></div>
      <div class="lib-stats" id="libStats"></div>
    </header>
    <div class="data-scope-bar" aria-label="数据口径说明">
      <div class="scope-main"><b>数据口径</b><span>仅收录优质上榜综合代理，能力沿用平台优质代理刻画结构</span></div>
      <div class="scope-items"><span><i>考核</i>按平台考核周期更新</span><span><i>体量</i>同类型代理相对站位，不披露具体消耗</span><span><i>当前</i>原型版，尚未接入自动刷新</span></div>
    </div>
    <div class="filter-bar" id="filterBar">
      <div class="filter-group" data-key="track"><span>类型</span></div>
      <div class="filter-group" data-key="industry"><span>行业</span></div>
      <div class="filter-group" data-key="cert"><span>认证</span></div>
      <div class="filter-search">
        <input id="libSearch" placeholder="搜索代理、品牌、案例、行业、能力标签…"/>
        <button class="btn btn-sm" id="clearFilters">清空</button>
      </div>
    </div>
    <div class="sort-row">
      <span>排序：</span>
      <button class="sort-btn active" data-sort="evidence">案例证据</button>
      <button class="sort-btn" data-sort="capability">能力站位</button>
      <button class="sort-btn" data-sort="scale">消耗规模</button>
      <span class="sort-tip" id="libCount"></span>
      <button class="btn btn-sm btn-cy" id="libToMatch">带筛选条件发起匹配</button>
    </div>
    <div class="lib-grid" id="libGrid"></div>
  </section>

  <!-- ════════ 智能匹配 ════════ -->
  <section class="page" id="page-match" hidden="">
    <header class="page-head">
      <div><h1>匹配参考</h1><p>客户需求 → 结构化 Brief → 证据优先推荐 → 内部比选清单 → 渠道终审</p></div>
      <div class="match-steps" id="matchSteps">
        <span class="ms active" data-ms="1">① 需求</span><i></i>
        <span class="ms" data-ms="2">② 站位推荐</span><i></i>
        <span class="ms" data-ms="3">③ 清单路由</span>
      </div>
    </header>

    <!-- step1 输入 -->
    <div class="match-step" id="mstep1">
      <div class="input-hero">
        <div class="input-hero-glow"></div>
        <h2>一句话描述客户需求</h2>
        <p>行业、品牌阶段、营销目标、预算——像和同事说话一样描述就行</p>
        <div class="input-wrap">
          <textarea id="needInput" rows="3" placeholder="例：客户是新锐婴儿辅食品牌，想做新品冷启动，月预算 30 万左右…"></textarea>
          <button class="btn btn-primary" id="parseBtn">解析 Brief</button>
        </div>
        <div class="quick-fill" id="quickFill"></div>
      </div>
      <div class="brief-card" id="briefCard" hidden="">
        <div class="brief-head">
          <h2>结构化 Brief</h2>
          <span>AI 不猜 · 每个字段可修改，确认后才进入匹配</span>
        </div>
        <div class="brief-grid" id="briefGrid"></div>
        <div class="brief-actions">
          <button class="btn btn-primary btn-lg" id="confirmBriefBtn">确认 Brief，生成推荐 →</button>
        </div>
      </div>
    </div>

    <!-- step2 推荐矩阵 -->
    <div class="match-step" id="mstep2" hidden="">
      <div class="brief-summary-bar">
        <div class="brief-summary" id="briefSummary"></div>
        <button class="btn btn-sm" id="backToBrief">← 修改 Brief</button>
      </div>
      <div class="podium" id="podium"></div>
      <div class="lane-note" id="laneNote" hidden=""></div>
      <div class="rest-title" id="restTitle"></div>
      <div class="board" id="board"></div>
      <div class="score-legend" id="scoreLegend">
        匹配分构成：<i class="seg seg-case"></i>类似案例 50% <i class="seg seg-cert"></i>认证 20% <i class="seg seg-cap"></i>能力 20% <i class="seg seg-budget"></i>预算 10%
        <span class="score-legend-note">案例相似度 = 行业 × 品牌阶段 × 营销目的 三重命中</span>
      </div>
      <div class="select-bar">
        <span id="selectInfo">尚未勾选意向代理</span>
        <div>
          <button class="btn btn-dark-ghost" id="addCompareBtn" disabled="">加入对比</button>
          <button class="btn btn-dark-ghost" id="toComplianceBtn">生成内部比选清单</button>
          <button class="btn btn-primary" id="toRouteBtn" disabled="">路由渠道终审 →</button>
        </div>
      </div>
    </div>

    <!-- step3 清单与路由 -->
    <div class="match-step" id="mstep3" hidden="">
      <div class="tabs" role="tablist">
        <button class="tab active" data-tab="compliance">内部比选清单（流转商服）</button>
        <button class="tab" data-tab="route">建联路由（直客 ⇄ 渠道）</button>
        <button class="btn btn-sm" id="backToMatrix" style="margin-left:auto">← 返回推荐</button>
      </div>
      <div class="tab-panel" id="panel-compliance">
        <div class="rule-banner" id="ruleBanner"></div>
        <div class="dual-pane">
          <div class="pane pane-internal">
            <div class="pane-head"><strong>内部参考版</strong><span>含排序与匹配分 · 仅直客/渠道可见</span></div>
            <ol class="internal-list" id="internalList"></ol>
          </div>
          <div class="pane-divider"><span>内部参考</span><i></i><span>转商服</span></div>
          <div class="pane pane-external">
            <div class="pane-head"><strong>商服复核清单草稿</strong><span>无排序 · 无分数 · 由商服按标准流程复核后对客发出</span></div>
            <div class="external-list" id="externalList"></div>
            <p class="external-foot" id="externalFoot"></p>
            <div class="handoff-bar">
              <button class="btn btn-primary" id="handoffBtn">一键流转商服（标准流程）</button>
              <span id="handoffState"></span>
            </div>
          </div>
        </div>
      </div>
      <div class="tab-panel" id="panel-route" hidden="">
        <div class="route-toolbar">
          <div class="view-toggle" role="group">
            <button class="active" data-view="da">直客视角</button>
            <button data-view="qu">渠道视角</button>
          </div>
          <span class="route-dest">路由目标：<b>渠道四组 · 母婴线</b>（不展示渠道经理个人信息）</span>
        </div>
        <div class="route-layout">
          <div class="route-main" id="routeMain"></div>
          <aside class="route-timeline">
            <h3>状态流转</h3>
            <ul id="timeline"></ul>
          </aside>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════ 对比分析 ════════ -->
  <section class="page" id="page-compare" hidden="">
    <header class="page-head">
      <div><h1>代理对比</h1><p>横向对比——能力罗盘、经营指标、案例证据一屏对齐</p></div>
      <button class="btn btn-sm" id="clearCompare">清空对比</button>
    </header>
    <div class="compare-picker" id="comparePicker"></div>
    <div class="compare-empty" id="compareEmpty">
      <div class="ce-icon">⇄</div>
      <p>从上方选择 2-3 家代理开始对比<br/><span>也可以在站位库或匹配结果里点「+对比」加入</span></p>
    </div>
    <div class="compare-area" id="compareArea" hidden="">
      <div class="compare-radar-row">
        <div class="panel radar-panel">
          <div class="panel-head"><h2>能力罗盘对比</h2><span>含大盘中位线对照</span></div>
          <svg id="radarSvg" viewBox="0 0 360 320"></svg>
          <div class="radar-legend" id="radarLegend"></div>
        </div>
        <div class="panel" style="flex:1">
          <div class="panel-head"><h2>经营指标对比</h2></div>
          <div class="compare-table-wrap"><table class="compare-table" id="compareTable"></table></div>
        </div>
      </div>
      <div class="panel">
        <div class="panel-head"><h2>案例证据对比</h2><span>谁真正做过类似客户，一眼见分晓</span></div>
        <div class="compare-cases" id="compareCases"></div>
      </div>
      <div class="compare-exit" id="compareExit"></div>
    </div>
  </section>

  <!-- ════════ 协同记录 ════════ -->
  <section class="page" id="page-insights" hidden="">
    <header class="page-head">
      <div><h1>协同记录</h1><p>确认动作 = 数据录入 · 用得越多越准</p></div>
    </header>
    <div class="insight-grid">
      <div class="panel">
        <div class="panel-head"><h2>数据飞轮</h2><span>五步闭环 · 两个高亮环是本产品新增的沉淀点</span></div>
        <svg viewBox="0 0 420 420" id="flywheelSvg" role="img"></svg>
      </div>
      <div class="insight-side">
        <div class="panel">
          <div class="panel-head"><h2>本次操作沉淀</h2></div>
          <ul id="sessionLog"><li class="empty">完成一次建联路由后，这里会出现真实沉淀的数据条目</li></ul>
        </div>
        <div class="panel">
          <div class="panel-head"><h2>Roadmap</h2></div>
          <div class="roadmap-steps">
            <div><b>① 冷启动</b><span>接入优质代理考核结果 + 平台经营数据</span></div>
            <div><b>② 热运转</b><span>承接状态 / 命中率 / 案例库随使用自然沉淀</span></div>
            <div><b>③ 远期</b><span>对客户侧输出脱敏版（合规评审后）</span></div>
          </div>
        </div>
      </div>
    </div>
    <div class="panel">
      <div class="panel-head"><h2>架构说明</h2><span>本工作台的数据流与机制说明</span></div>
      <div style="font-size:13px;color:var(--tx-2);line-height:1.8;padding:8px 4px">
        <p><b>数据来源</b>：优质代理考核结果（certs.youzhi=true）+ 平台经营数据映射，能力罗盘沿用平台优质代理刻画结构；正式接入后由平台经营数据落位。</p>
        <p style="margin-top:8px"><b>匹配机制</b>：需求Brief → 硬条件过滤（行业覆盖/预算门槛）→ 软打分（案例50% + 认证20% + 能力20% + 预算10%）→ 内部比选清单 → 路由渠道确认。</p>
        <p style="margin-top:8px"><b>对客输出路径</b>：内部比选清单 → 转商服按标准推荐流程补齐复核 → 对客发出。本池仅含综合4A/本土两类优质代理，不宣称直接满足≥3赛道达标。</p>
        <p style="margin-top:8px"><b>能力罗盘</b>：六维（消耗规模、好策略、头客服务、客户留存、种销双投、搜索能力），以大盘中位线为对比基准。</p>
        <p style="margin-top:8px"><b>案例分类</b>：每条案例标注&#34;种草+开环/闭环case&#34;或&#34;腰部客户跃迁case&#34;类别，在详情/对比/推荐理由中展示。</p>
      </div>
    </div>
  </section>

</main>

<!-- ======== 代理详情抽屉 ======== -->
<div class="drawer-mask" id="drawerMask" hidden=""></div>
<aside class="drawer" id="drawer" hidden="">
  <div class="drawer-inner" id="drawerInner"></div>
</aside>

<div class="toast-wrap" id="toastWrap"></div>

<script>/* 直渠握手·优质代理推荐工作台 — 数据层 v9
   署名：林昕悦（实习）
   代理池仅保留优质上榜代理（certs.youzhi=true），类型仅综合4A/本土综合。
   体量不披露具体值，只显示行业高位/中位/低位。
   案例标注"种草+开环/闭环case"和"腰部客户跃迁case"类别。
*/

const TRACKS = {
  "综合4A":   { color: "#6ea8ff", key: "4a" },
  "本土综合": { color: "#37d0c5", key: "local" }
};

const BUDGET_TIERS = { micro: "10w以内", small: "10-30w", mid: "30-100w", large: "100w以上" };

/* 优质上榜代理（certs.youzhi=true）：综合4A×3 · 本土综合×4 */
const AGENTS = [
  {
    id:"A01", name:"铂雅传播", track:"综合4A", hue:212,
    certs:{youzhi:true,lingxi:true},
    industries:["美妆","奢品","食品饮料","母婴","3C数码"],
    objectives:["品牌心智","种草效果化"], brandStages:["成熟"],
    capability:{scale:95,strategy:90,clientSvc:88,retention:92,dualInvest:78,search:72},
    budgetFit:[100,500], intake:"open",
    founded:2009, hq:"上海", teamSize:420, redTeam:58, clientCount:36, renewRate:88, selfOpRate:31,
    scaleTier:"高位",
    tags:["年框大客","品牌大事件","IP整合","AI营销领航","双赛道优质"],
    clients:["璃光彩妆","漾白护肤","晏泊腕表","汽跃饮料","维达纸品","嘉士伯啤酒","乐事薯片"],
    intro:"头部科技营销集团，拥有75+专利软著和自研营销大模型，小红书双赛道优质服务商，擅长品牌心智大事件与种收一体整合。",
    svc:["品牌全案","IP整合营销","KOS体系搭建","AI内容效果预测"], tools:"自研营销大模型「前成」（双备案）+AI选号Agent，覆盖蒲公英13.3w达人实时数据", kol:"与99%+MCN战略合作，尾部优质达人建联6w+（蒲公英2000元以下8w+中的6w+），可当天供号",
    cases:[
      {industry:"美妆",category:"高端护肤",stage:"成熟",objective:"品牌心智",tier:"large",brand:"漾白护肤",period:"2026 H1",result:"年框客户品牌大事件，站内声量行业Top1",caseType:"种草+开环/闭环case"},
      {industry:"食品饮料",category:"纸品",stage:"成熟",objective:"品牌心智",tier:"large",brand:"维达纸品",period:"2025 H2",result:"10年沉淀品牌超级IP，金投赏Top1+IAI金奖，AI变装互动引爆",caseType:"种草+开环/闭环case"},
      {industry:"食品饮料",category:"碳酸饮料",stage:"成熟",objective:"种草效果化",tier:"large",brand:"汽跃饮料",period:"2026 H1",result:"种收一体campaign，回搜率超行业bench 40%",caseType:"种草+开环/闭环case"},
      {industry:"食品饮料",category:"休闲零食",stage:"成熟",objective:"种草效果化",tier:"large",brand:"乐事薯片",period:"2026 H1",result:"新品90天0-1打爆，竞品人群渗透率81%+，拉新率80%优于行业Top5",caseType:"种草+开环/闭环case"}
    ]
  },
  {
    id:"A02", name:"众合广告", track:"综合4A", hue:224,
    certs:{youzhi:true,lingxi:false},
    industries:["母婴","食品饮料","家清个护","美妆个护","3C电器","服装配饰"],
    objectives:["种草效果化","竞价效果"], brandStages:["成熟","成长"],
    capability:{scale:92,strategy:78,clientSvc:82,retention:84,dualInvest:90,search:88},
    budgetFit:[50,300], intake:"open",
    founded:2012, hq:"武汉", teamSize:260, redTeam:44, clientCount:41, renewRate:84, selfOpRate:57,
    scaleTier:"高位",
    tags:["KFS方法论首创","效果导向","长期TOP1","四级种草体系","投后陪跑"],
    clients:["牧遥奶粉","棉初纸品","净屿洗护","禾乐冲饮","科沃斯机器人","丸美面霜","雪玲妃洁面","SIINSIIN鲨鱼裤"],
    intro:"小红书商业化全程参与者与共创者，KFS方法论推出者，累计操盘10亿+体量长期TOP1代理商，四级种草服务体系覆盖全行业。",
    svc:["效果投放","种收一体运营","投后陪跑","多SPU精细化管理"], tools:"自研三大科技产品：智能投放工作台 + AI营销助手 + CDP客户体验平台，行业KnowHow覆盖7397个细分SPU", kol:"全行业达人资源网络，母婴/美妆/食饮垂类深度建联",
    cases:[
      {industry:"母婴",category:"婴幼儿奶粉",stage:"成熟",objective:"种草效果化",tier:"large",brand:"牧遥奶粉",period:"2026 H1",result:"KFS组合投放，小红星ROI较接手前提升35%",caseType:"种草+开环/闭环case"},
      {industry:"家清个护",category:"清洁电器",stage:"成熟",objective:"种草效果化",tier:"large",brand:"科沃斯机器人",period:"2025 H2",result:"多SPU分品定位分阶投放，AIPS人群渗透行业Top1，品类搜索Top1",caseType:"种草+开环/闭环case"},
      {industry:"母婴",category:"婴童纸品",stage:"成熟",objective:"竞价效果",tier:"mid",brand:"棉初纸品",period:"2025 H2",result:"竞价日耗稳定爬坡3倍，CPC低于行业均值",caseType:"腰部客户跃迁case"},
      {industry:"美妆个护",category:"面霜",stage:"成熟",objective:"种草效果化",tier:"mid",brand:"丸美面霜",period:"2026 H1",result:"正反漏斗模型引爆新品，新品登顶电商平台面霜品类Top1",caseType:"种草+开环/闭环case"}
    ]
  },
  {
    id:"A04", name:"华略国际", track:"综合4A", hue:236,
    certs:{youzhi:true,lingxi:true},
    industries:["美妆","奢品","服饰潮流"],
    objectives:["品牌心智","种草效果化"], brandStages:["成熟"],
    capability:{scale:92,strategy:85,clientSvc:86,retention:90,dualInvest:68,search:66},
    budgetFit:[80,400], intake:"tight",
    founded:2008, hq:"上海", teamSize:350, redTeam:39, clientCount:28, renewRate:90, selfOpRate:28,
    scaleTier:"高位",
    tags:["奢品头客","国际客户","提案能力强"],
    clients:["珂珞奢品","雾时香氛","叙白服饰"],
    intro:"国际客户为主的综合4A，奢品与高端美妆提案能力突出。",
    svc:["品牌全案","国际客户服务","大事件营销"], tools:"全球媒介数据管理平台", kol:"高奢时尚头部达人直签",
    cases:[
      {industry:"奢品",category:"香氛",stage:"成熟",objective:"品牌心智",tier:"large",brand:"雾时香氛",period:"2026 H1",result:"节日campaign站内声量类目第一",caseType:"种草+开环/闭环case"},
      {industry:"美妆",category:"高端彩妆",stage:"成熟",objective:"种草效果化",tier:"large",brand:"珂珞奢品",period:"2025 H2",result:"KFS+KOS组合，搜索渗透显著提升",caseType:"种草+开环/闭环case"}
    ]
  },
  {
    id:"B01", name:"沐光互动", track:"本土综合", hue:168,
    certs:{youzhi:true,lingxi:true},
    industries:["母婴","食品饮料"],
    objectives:["新品冷启动","种草效果化"], brandStages:["新锐","成长"],
    capability:{scale:58,strategy:87,clientSvc:78,retention:81,dualInvest:74,search:82},
    budgetFit:[10,60], intake:"open",
    founded:2018, hq:"杭州", teamSize:85, redTeam:32, clientCount:22, renewRate:81, selfOpRate:74,
    scaleTier:"中位",
    tags:["冷启动方法论","母婴垂类","内容策略","爆文模型"],
    clients:["禾语辅食","小杞辅食","果柚零食"],
    intro:"专注母婴与食饮新锐品牌的策略内容型代理，冷启动方法论成熟。",
    svc:["内容种草","冷启动全案","策略咨询"], tools:"自研母婴爆文选题库+笔记预审模型", kol:"母婴尾部优质达人8000+建联，可当天供号",
    cases:[
      {industry:"母婴",category:"婴幼儿辅食",stage:"新锐",objective:"新品冷启动",tier:"small",brand:"禾语辅食",period:"2026 H1",result:"0-1冷启动，6周起量，进辅食类目搜索Top10",caseType:"腰部客户跃迁case"},
      {industry:"母婴",category:"婴幼儿辅食",stage:"新锐",objective:"新品冷启动",tier:"mid",brand:"小杞辅食",period:"2025 H2",result:"新品上市3个月，爆文率21%，站内NPS类目第一",caseType:"腰部客户跃迁case"},
      {industry:"母婴",category:"婴童零食",stage:"成长",objective:"种草效果化",tier:"small",brand:"果柚零食",period:"2026 H1",result:"小预算种收联动，小红星ROI达标",caseType:"种草+开环/闭环case"}
    ]
  },
  {
    id:"B02", name:"澜途文化", track:"本土综合", hue:152,
    certs:{youzhi:true,lingxi:false},
    industries:["母婴","服饰潮流"],
    objectives:["新品冷启动","小预算试水","种草效果化"], brandStages:["新锐","成长"],
    capability:{scale:52,strategy:72,clientSvc:84,retention:86,dualInvest:62,search:70},
    budgetFit:[5,30], intake:"open",
    founded:2019, hq:"广州", teamSize:46, redTeam:21, clientCount:18, renewRate:86, selfOpRate:79,
    scaleTier:"低位",
    tags:["小预算友好","带教陪跑","素人矩阵","婴童服饰"],
    clients:["棉朵亲子","叙暖童装","苗禾服饰"],
    intro:"内容与投后见长的小型代理，擅长带教零经验团队从小预算起步。",
    svc:["内容种草","带教陪跑","素人矩阵运营"], tools:"平台标准工具，人工精细化运营", kol:"素人+KOC矩阵为主，长期口碑沉淀",
    cases:[
      {industry:"母婴",category:"婴童服饰",stage:"新锐",objective:"小预算试水",tier:"micro",brand:"棉朵亲子",period:"2026 H1",result:"月耗8w起步，3个月跑通内容模型后客户加预算至25w",caseType:"腰部客户跃迁case"},
      {industry:"母婴",category:"婴童服饰",stage:"成长",objective:"种草效果化",tier:"small",brand:"叙暖童装",period:"2025 H2",result:"站内种草+电商承接，进店率超bench",caseType:"种草+开环/闭环case"},
      {industry:"服饰潮流",category:"童装亲子装",stage:"新锐",objective:"新品冷启动",tier:"small",brand:"苗禾服饰",period:"2026 H1",result:"素人+腰部KOC组合，单篇爆文带动店铺访客峰值10倍",caseType:"腰部客户跃迁case"}
    ]
  },
  {
    id:"B03", name:"星桥传媒", track:"本土综合", hue:184,
    certs:{youzhi:true,lingxi:true},
    industries:["母婴","家清个护","食品饮料"],
    objectives:["种草效果化","竞价效果"], brandStages:["成长","成熟"],
    capability:{scale:80,strategy:76,clientSvc:80,retention:83,dualInvest:92,search:88},
    budgetFit:[30,200], intake:"full",
    founded:2015, hq:"上海", teamSize:150, redTeam:48, clientCount:29, renewRate:83, selfOpRate:68,
    scaleTier:"高位",
    tags:["投流强项","效果化标杆","好策略达标","多SPU矩阵"],
    clients:["牧遥奶粉","孕禾营养","泡语洗护"],
    intro:"投流运营突出的本土头部代理，母婴效果化项目经验密集。",
    svc:["效果投放","闭环电商直播","店播代运营"], tools:"自研投放实时监控看板", kol:"母婴/家清腰部达人库+买手直播资源",
    cases:[
      {industry:"母婴",category:"婴幼儿奶粉",stage:"成熟",objective:"种草效果化",tier:"large",brand:"牧遥奶粉",period:"2025 H2",result:"全年种收一体运营，效果化钱包份额提升，客户升框",caseType:"种草+开环/闭环case"},
      {industry:"母婴",category:"孕产营养",stage:"成长",objective:"竞价效果",tier:"mid",brand:"孕禾营养",period:"2026 H1",result:"竞价冷启动30天达标，客资成本低于目标20%",caseType:"种草+开环/闭环case"},
      {industry:"家清个护",category:"母婴洗护",stage:"成熟",objective:"种草效果化",tier:"mid",brand:"泡语洗护",period:"2026 H1",result:"多SPU矩阵投放，好策略达标率组内第一",caseType:"腰部客户跃迁case"}
    ]
  },
  {
    id:"B06", name:"巨浪营销", track:"本土综合", hue:8,
    certs:{youzhi:true,lingxi:false},
    industries:["食品饮料","数码家电"],
    objectives:["种草效果化","竞价效果"], brandStages:["成长","成熟"],
    capability:{scale:74,strategy:68,clientSvc:70,retention:80,dualInvest:85,search:72},
    budgetFit:[20,150], intake:"open",
    founded:2016, hq:"深圳", teamSize:110, redTeam:30, clientCount:26, renewRate:80, selfOpRate:64,
    scaleTier:"中位",
    tags:["食饮大盘","竞价爬坡","电商承接"],
    clients:["晨屿咖啡","麦知烘焙","风移家电"],
    intro:"食饮与数码见长的效果型代理，竞价起量与电商承接经验丰富。",
    svc:["效果投放","竞价代投"], tools:"批量投放脚本工具", kol:"以信息流为主，达人资源较薄",
    cases:[
      {industry:"食品饮料",category:"即饮咖啡",stage:"成长",objective:"种草效果化",tier:"mid",brand:"晨屿咖啡",period:"2026 H1",result:"种收联动新品季，进店率类目Top5",caseType:"种草+开环/闭环case"},
      {industry:"数码家电",category:"厨房小家电",stage:"成熟",objective:"竞价效果",tier:"mid",brand:"风移家电",period:"2025 H2",result:"大促竞价日耗翻两倍，成本达标",caseType:"腰部客户跃迁case"}
    ]
  }
];

/* 三个演示场景 */
const SCENARIOS = [
  {
    id:"s1", chip:"新锐辅食冷启动", icon:"🍼",
    desc:"A轮新锐辅食品牌 · 新品冷启动 · 月耗30w",
    point:"看点：证据优先——小而美案例党顶掉六维全A大代理",
    input:"客户是新锐婴儿辅食品牌，去年刚拿A轮，今年主推一款有机米粉新品，想在小红书做冷启动，月预算30万左右，客户团队有基础投放经验。",
    brief:{ industry:"母婴", category:"婴幼儿辅食", stage:"新锐", objective:"新品冷启动", budget:30, form:"种草为主", special:"无", source:"自投转代投" },
    routePick:"B01",
    routeScript:{ type:"accept", note:"团队有辅食冷启动完整方法论，9月前可开新项目组。" }
  },
  {
    id:"s2", chip:"成熟奶粉效果化", icon:"🥛",
    desc:"成熟奶粉品牌 · 种草效果化 · 月耗120w · Q4档期",
    point:"看点：分数第一但满载——档期冲突→渠道建议换家",
    input:"成熟奶粉品牌，在站内投了三年，今年KPI是种草效果化，要看小红星ROI，月预算120万，Q4是重点，希望代理投流和投后能力强。",
    brief:{ industry:"母婴", category:"婴幼儿奶粉", stage:"成熟", objective:"种草效果化", budget:120, form:"种收一体", special:"投流+投后强，Q4档期", source:"二开（更换代理）" },
    routePick:"B03",
    routeScript:{
      type:"conflict",
      reason:"Q4大促项目满载，无法保障新客户投放质量",
      altId:"A02",
      altReason:"同样有成熟奶粉效果化案例（小红星ROI+35%），投流投后配置齐，Q4有承接余量"
    }
  },
  {
    id:"s3", chip:"母婴服饰小预算", icon:"👶",
    desc:"婴童服饰新锐 · 小预算试水 · 月耗10w内 · 零经验团队",
    point:"看点：带教型路线，AI 不硬拍板",
    input:"客户做婴童服饰，天猫起家想试试小红书，月预算就10万以内，团队完全没投过小红书，需要代理多带一带。",
    brief:{ industry:"母婴", category:"婴童服饰", stage:"新锐", objective:"小预算试水", budget:9, form:"种草试水", special:"客户无投放经验，需强投后服务", source:"新客首次代投" },
    routePick:"B02",
    routeScript:{ type:"accept", note:"能接。备注：澜途9月排期已半满，建议8月内启动。" }
  }
];

/* 承接状态元信息 */
const INTAKE_META = {
  A01:{days:2,by:"渠道确认"},  A02:{days:1,by:"渠道确认"},  A04:{days:9,by:"渠道确认"},
  B01:{days:0,by:"今日路由确认"}, B02:{days:1,by:"渠道确认"},  B03:{days:3,by:"渠道确认"},
  B06:{days:8,by:"渠道确认"}
};

/* 在服品牌 → 品类映射（用于竞品冲突检测）*/
const BRAND_CATEGORY = {
  "璃光彩妆":"彩妆","漾白护肤":"护肤","晏泊腕表":"腕表珠宝","汽跃饮料":"饮料",
  "牧遥奶粉":"婴幼儿奶粉","棉初纸品":"婴童纸品","净屿洗护":"洗护","禾乐冲饮":"滋补冲饮",
  "珂珞奢品":"高端彩妆","雾时香氛":"香氛","叙白服饰":"服饰",
  "禾语辅食":"婴幼儿辅食","小杞辅食":"婴幼儿辅食","果柚零食":"婴童零食",
  "棉朵亲子":"婴童服饰","叙暖童装":"婴童服饰","苗禾服饰":"婴童服饰",
  "孕禾营养":"孕产营养","泡语洗护":"母婴洗护",
  "晨屿咖啡":"即饮咖啡","麦知烘焙":"烘焙","风移家电":"家电"
};

/* 能力罗盘维度（v9：沿用优质代理刻画结构） */
const CAP_NAMES = {
  scale:"消耗规模",
  strategy:"好策略",
  clientSvc:"头客服务",
  retention:"客户留存",
  dualInvest:"种销双投",
  search:"搜索能力"
};

/* 行业体量分位标签 */
function scaleLabel(tier) {
  return tier === "高位" ? "行业高位" : tier === "中位" ? "行业中位" : "行业低位";
}
</script>
<script>/* 直渠握手·优质代理推荐工作台 — 匹配引擎 v9
   纯函数，可独立验证。署名：林昕悦（实习）
   代理池仅优质上榜代理，类型仅综合4A/本土综合。
   合规清单改为内部比选清单/路由渠道确认，不再宣称≥3赛道达标。
*/

function parseBrief(text) {
  const t = text.trim();
  for (const s of SCENARIOS) {
    if (t === s.input) return Object.assign({}, s.brief, { _scenario: s.id, _missing: [] });
  }
  const brief = { industry:"母婴", category:"", stage:"新锐", objective:"种草效果化", budget:0, form:"种草为主", special:"无", source:"新客首次代投" };
  const missing = [];
  const industries = { "母婴":["母婴","辅食","奶粉","婴","童","亲子","孕"], "美妆":["美妆","彩妆","护肤"], "食品饮料":["食品","饮料","零食","乳"], "服饰潮流":["服饰","服装","穿搭"], "家清个护":["家清","洗护","日化"], "数码家电":["数码","家电"], "汽车出行":["汽车","车企"] };
  let indHit = false;
  for (const ind in industries) { if (industries[ind].some(k => t.includes(k))) { brief.industry = ind; indHit = true; break; } }
  if (!indHit) missing.push("industry");
  if (/新锐|初创|刚起步|A轮|天使轮|0-1|冷启动/.test(t)) brief.stage = "新锐";
  else if (/成熟|投了.*年|大牌|头部/.test(t)) brief.stage = "成熟";
  else if (/成长|扩张/.test(t)) brief.stage = "成长";
  else missing.push("stage");
  if (/冷启动|新品上市|0-1/.test(t)) brief.objective = "新品冷启动";
  else if (/效果化|小红星|ROI|种收/.test(t)) brief.objective = "种草效果化";
  else if (/试水|试试|尝试/.test(t)) brief.objective = "小预算试水";
  else if (/竞价|客资|线索/.test(t)) brief.objective = "竞价效果";
  else if (/心智|声量|品牌/.test(t)) brief.objective = "品牌心智";
  else missing.push("objective");
  const m = t.match(/(\d+)\s*[万w]/i);
  if (m) brief.budget = parseInt(m[1], 10);
  else { brief.budget = 30; missing.push("budget"); }
  if (/没投过|无经验|不会投|带一带/.test(t)) brief.special = "客户无投放经验，需强投后服务";
  if (/换代理|换家|二开|现在的代理/.test(t)) brief.source = "二开（更换代理）";
  else if (/自己投|自投|转代投/.test(t)) brief.source = "自投转代投";
  else if (/SME|生态|自助/.test(t)) brief.source = "生态/SME客户";
  const catM = t.match(/(辅食|奶粉|纸品|婴童服饰|童装|零食|彩妆|护肤|洗护)/);
  if (catM) brief.category = catM[1];
  brief._missing = missing;
  return brief;
}

function budgetTier(budget) {
  if (budget < 10) return "micro";
  if (budget <= 30) return "small";
  if (budget <= 100) return "mid";
  return "large";
}

function hardFilter(agent, brief) {
  const reasons = [];
  if (!agent.industries.includes(brief.industry)) reasons.push("不覆盖" + brief.industry + "行业");
  if (brief.budget < agent.budgetFit[0]) reasons.push("预算门槛 " + agent.budgetFit[0] + "w 起，客户月耗 " + brief.budget + "w 不适配");
  if (brief.budget > agent.budgetFit[1] * 1.2) reasons.push("预算区间上限 " + agent.budgetFit[1] + "w，承接 " + brief.budget + "w 体量有风险");
  return { pass: reasons.length === 0, reasons: reasons };
}

function caseScore(agent, brief) {
  let best = 0, bestCase = null, tripleCount = 0;
  for (const c of agent.cases) {
    let hits = 0;
    if (c.industry === brief.industry) hits++;
    if (c.stage === brief.stage) hits++;
    if (c.objective === brief.objective) hits++;
    if (c.industry !== brief.industry) hits = Math.min(hits, 0);
    let s = hits === 3 ? 100 : hits === 2 ? 70 : hits === 1 ? 40 : 0;
    if (hits === 3) tripleCount++;
    if (s > best) { best = s; bestCase = c; }
  }
  if (tripleCount >= 2) best = Math.min(best + 10, 100);
  return { score: best, bestCase: bestCase, tripleCount: tripleCount };
}

function certScore(agent) {
  const youzhi = agent.certs.youzhi, lingxi = agent.certs.lingxi;
  if (youzhi && lingxi) return 100;
  if (youzhi) return 70;
  if (lingxi) return 50;
  return 20;
}

const CAP_WEIGHTS = {
  "新品冷启动": { strategy:.35, clientSvc:.2, dualInvest:.15, search:.1, scale:.1, retention:.1 },
  "种草效果化": { dualInvest:.3, search:.25, strategy:.15, retention:.1, scale:.1, clientSvc:.1 },
  "小预算试水": { clientSvc:.3, strategy:.2, retention:.2, search:.1, scale:.05, dualInvest:.15 },
  "竞价效果":   { dualInvest:.3, search:.25, scale:.15, retention:.1, clientSvc:.1, strategy:.1 },
  "品牌心智":   { strategy:.35, scale:.2, dualInvest:.15, search:.1, clientSvc:.1, retention:.1 }
};

function capScore(agent, brief) {
  const base = CAP_WEIGHTS[brief.objective] || CAP_WEIGHTS["种草效果化"];
  let w = Object.assign({}, base);
  if (brief.source === "自投转代投" || /无投放经验/.test(brief.special || "")) {
    const boost = 0.12;
    w.clientSvc = (w.clientSvc || 0) + boost;
    const others = Object.keys(w).filter(k => k !== "clientSvc");
    others.forEach(k => { w[k] = w[k] * (1 - boost); });
  }
  let s = 0;
  for (const k in w) s += (agent.capability[k] || 0) * w[k];
  return Math.round(s);
}

function budgetScore(agent, brief) {
  const lo = agent.budgetFit[0], hi = agent.budgetFit[1], b = brief.budget;
  if (b < lo || b > hi) return 40;
  const mid = (lo + hi) / 2, span = (hi - lo) / 2;
  const dist = Math.abs(b - mid) / span;
  return dist < 0.5 ? 100 : 60 + Math.round((1 - dist) * 80);
}

const DEFAULT_WEIGHTS = { case:.5, cert:.2, cap:.2, budget:.1 };

function matchAgent(agent, brief, weights) {
  weights = weights || DEFAULT_WEIGHTS;
  const hf = hardFilter(agent, brief);
  const cs = caseScore(agent, brief);
  const cert = certScore(agent);
  const cap = capScore(agent, brief);
  const bud = budgetScore(agent, brief);
  const w = weights;
  const total = hf.pass ? Math.round(cs.score * w.case + cert * w.cert + cap * w.cap + bud * w.budget) : 0;
  return {
    agent: agent, pass: hf.pass, deadReasons: hf.reasons,
    total: total,
    parts: { case: cs.score, cert: cert, cap: cap, budget: bud },
    weighted: { case: cs.score * w.case, cert: cert * w.cert, cap: cap * w.cap, budget: bud * w.budget },
    bestCase: cs.bestCase, tripleCount: cs.tripleCount
  };
}

function runMatch(brief, weights) {
  weights = weights || DEFAULT_WEIGHTS;
  const results = AGENTS.map(a => matchAgent(a, brief, weights));
  const alive = results.filter(r => r.pass).sort((a, b) => b.total - a.total);
  const dead = results.filter(r => !r.pass);
  return { alive: alive, dead: dead, all: results };
}

function buildReasons(r, brief) {
  const a = r.agent, out = [];
  if (r.bestCase && r.parts.case >= 70) {
    const c = r.bestCase;
    const hitDesc = r.parts.case >= 100 ? "完全同类" : "相邻类型";
    const typeTag = c.caseType ? "【" + c.caseType + "】" : "";
    out.push({ icon:"📌", strong:true, text: hitDesc + "案例" + typeTag + "：" + c.category + "·" + c.stage + "品牌·" + c.objective + "——" + c.result });
    if (r.tripleCount >= 2) out.push({ icon:"📌", strong:true, text: "同类完全命中案例共 " + r.tripleCount + " 条，非孤例" });
  } else if (r.bestCase) {
    const typeTag = r.bestCase.caseType ? "【" + r.bestCase.caseType + "】" : "";
    out.push({ icon:"📎", strong:false, text: "仅行业相关案例" + typeTag + "：" + r.bestCase.category + "——" + r.bestCase.result + "（无" + brief.stage + "×" + brief.objective + "直接经验）" });
  } else {
    out.push({ icon:"⚠️", strong:false, text: "暂无" + brief.industry + "行业案例记录" });
  }
  const youzhi = a.certs.youzhi, lingxi = a.certs.lingxi;
  if (youzhi && lingxi) out.push({ icon:"✅", text:"优质代理 + 灵犀 双认证" });
  else if (youzhi) out.push({ icon:"✅", text:"优质代理认证（灵犀未认证）" });
  else if (lingxi) out.push({ icon:"☑️", text:"灵犀认证（未入优质代理）" });
  const w = CAP_WEIGHTS[brief.objective];
  const topDims = Object.entries(w).sort((x, y) => y[1] - x[1]).slice(0, 2).map(e => e[0]);
  const dimNames = CAP_NAMES;
  const dimDesc = topDims.map(d => dimNames[d] + " " + grade(a.capability[d]) + " 档").join(" / ");
  out.push({ icon:"🎯", text: brief.objective + "关键能力：" + dimDesc });
  out.push({ icon:"💰", text: "预算区间 " + a.budgetFit[0] + "-" + a.budgetFit[1] + "w，客户月耗 " + brief.budget + "w " + (r.parts.budget >= 80 ? "落在舒适区" : "可承接") });
  return out;
}

function grade(v) { return v >= 85 ? "A" : v >= 70 ? "B" : v >= 55 ? "C" : "D"; }

function competitorCheck(agent, brief) {
  if (!brief.category) return { conflict: false, brands: [] };
  const hits = [];
  for (const b of agent.clients) {
    const cat = BRAND_CATEGORY[b];
    if (!cat) continue;
    if (cat === brief.category) hits.push({ brand: b, cat: cat, level: "same" });
    else if (brief.category.includes("婴") && cat.includes("婴") && cat !== brief.category) hits.push({ brand: b, cat: cat, level: "near" });
  }
  const same = hits.filter(h => h.level === "same");
  return { conflict: same.length > 0, near: hits.filter(h => h.level === "near"), brands: same };
}

function budgetDistance(agent, brief) {
  const lo = agent.budgetFit[0], hi = agent.budgetFit[1], b = brief.budget;
  if (b >= lo && b <= hi) return 0;
  return b < lo ? lo - b : b - hi;
}

function buildShortlist(matchResult, brief) {
  const byTrack = {};
  const add = function(r, note) {
    const tk = r.agent.track;
    if (!byTrack[tk]) byTrack[tk] = [];
    if (!byTrack[tk].some(x => x.agent.id === r.agent.id)) byTrack[tk].push(Object.assign({}, r, { note: note }));
  };
  matchResult.alive.forEach(r => add(r, null));
  const trackNames = Object.keys(TRACKS);
  for (const tk of trackNames) {
    const cur = byTrack[tk] ? byTrack[tk].length : 0;
    if (cur > 0 && cur < 2) {
      const candidates = matchResult.dead
        .filter(r => r.agent.track === tk && r.agent.industries.includes(brief.industry))
        .sort((a, b) => budgetDistance(a.agent, brief) - budgetDistance(b.agent, brief));
      if (candidates.length) add(candidates[0], "预算档相邻，供扩大比选");
    }
  }
  const totalCount = Object.values(byTrack).reduce((s, arr) => s + arr.length, 0);
  const trackCount = Object.keys(byTrack).length;
  const supplemented = Object.values(byTrack).some(arr => arr.some(x => x.note));
  return { byTrack: byTrack, totalCount: totalCount, trackCount: trackCount, supplemented: supplemented,
    perTrackOk: Object.values(byTrack).every(arr => arr.length >= 2) };
}
</script>
<script>/* 直渠握手·优质代理推荐工作台 — 交互层 v9
   署名：林昕悦（实习）
   液态玻璃视觉 + 核心交互可运行
*/

const $ = (s, el = document) => el.querySelector(s);
const $$ = (s, el = document) => [...el.querySelectorAll(s)];

const state = {
  page: "home",
  brief: null, match: null, picked: new Set(),
  scenario: null, routeStage: 0, routeDecision: null, timeline: [],
  compare: new Set(), sessionLogs: [],
  libFilters: { track: new Set(), industry: new Set(), objective: new Set(), cert: new Set(), intake: new Set() },
  libSort: "evidence", libSearch: ""
};

const INTAKE_MAP = { open:["intake-open","● 可接"], tight:["intake-tight","▲ 档期紧张"], full:["intake-full","■ 满载"] };

function intakeFresh(a, short) {
  const meta = INTAKE_META[a.id];
  if (!meta) return "";
  const when = meta.days === 0 ? "今天" : meta.days === 1 ? "昨天" : meta.days + "天前";
  return short ? when + "确认" : when + " · " + meta.by;
}
function intakeBadge(a) {
  const ic = INTAKE_MAP[a.intake];
  return '<span class="intake ' + ic[0] + '" title="' + intakeFresh(a) + '">' + ic[1] + '<em class="intake-fresh">' + intakeFresh(a, true) + '</em></span>';
}

function nav(page) {
  state.page = page;
  $$(".nav-list button").forEach(b => b.classList.toggle("active", b.dataset.page === page));
  $$(".page").forEach(p => p.hidden = p.id !== "page-" + page);
  window.scrollTo({ top: 0 });
  if (page === "home") renderHome();
  if (page === "library") renderLibrary();
  if (page === "compare") renderCompare();
  if (page === "insights") renderInsights();
}

function countUp(el, target, suffix, dur) {
  dur = dur || 900; suffix = suffix || "";
  const start = performance.now();
  const step = t => {
    const p = Math.min(1, (t - start) / dur);
    const eased = 1 - Math.pow(1 - p, 3);
    el.textContent = Math.round(target * eased) + suffix;
    if (p < 1) requestAnimationFrame(step);
  };
  requestAnimationFrame(step);
}

function renderHome() {
  $("#homeScenarios").innerHTML = SCENARIOS.map(s => 
    '<button class="scenario-card" data-sc="' + s.id + '">' +
    '<span class="sc-icon">' + s.icon + '</span>' +
    '<span class="sc-body"><b>' + s.chip + '</b><span>' + s.desc + '</span><em>' + s.point + '</em></span>' +
    '<span class="sc-go">→</span></button>'
  ).join("");
  $$("#homeScenarios .scenario-card").forEach(c => c.onclick = () => { nav("match"); loadScenario(c.dataset.sc); });
}

const FILTER_OPTS = {
  track: Object.keys(TRACKS),
  industry: ["母婴","美妆","食品饮料","家清个护","服饰潮流","数码家电","奢品","运动户外"],
  objective: ["新品冷启动","种草效果化","小预算试水","竞价效果","品牌心智"],
  cert: ["优质代理","灵犀认证"],
  intake: ["可接","档期紧张","满载"]
};

function initFilters() {
  $$(".filter-group").forEach(g => {
    const key = g.dataset.key;
    FILTER_OPTS[key].forEach(opt => {
      const b = document.createElement("button");
      b.className = "fchip"; b.textContent = opt;
      b.onclick = () => {
        if (state.libFilters[key].has(opt)) state.libFilters[key].delete(opt);
        else state.libFilters[key].add(opt);
        renderLibrary();
      };
      g.appendChild(b);
    });
  });
  $("#libSearch").oninput = e => { state.libSearch = e.target.value.trim(); renderLibrary(); };
  $("#clearFilters").onclick = () => {
    Object.values(state.libFilters).forEach(s => s.clear());
    state.libSearch = ""; $("#libSearch").value = "";
    renderLibrary();
  };
  $$(".sort-btn").forEach(b => b.onclick = () => {
    state.libSort = b.dataset.sort;
    $$(".sort-btn").forEach(x => x.classList.toggle("active", x === b));
    renderLibrary();
  });
}

function libMatch(a) {
  const f = state.libFilters;
  if (f.track.size && !f.track.has(a.track)) return false;
  if (f.industry.size && ![...f.industry].some(i => a.industries.includes(i))) return false;
  if (f.objective.size && ![...f.objective].some(o => a.objectives.includes(o))) return false;
  if (f.cert.size) {
    const tags = [];
    if (a.certs.youzhi) tags.push("优质代理");
    if (a.certs.lingxi) tags.push("灵犀认证");
    if (![...f.cert].some(c => tags.includes(c))) return false;
  }
  if (f.intake.size) {
    const m = { open:"可接", tight:"档期紧张", full:"满载" };
    if (!f.intake.has(m[a.intake])) return false;
  }
  if (state.libSearch) {
    const terms = state.libSearch.toLowerCase().split(/\s+/).filter(Boolean);
    const caseFields = a.cases.flatMap(c => [c.brand, c.category, c.industry, c.stage, c.objective, c.result, c.caseType, c.period]);
    const capabilityFields = Object.entries(a.capability).flatMap(([k, v]) => [CAP_NAMES[k], grade(v)]);
    const hay = [a.name, a.hq, a.track, a.intro, a.tools, a.kol,
      ...a.tags, ...a.clients, ...a.industries, ...a.objectives, ...a.brandStages,
      ...(a.svc || []), ...caseFields, ...capabilityFields
    ].filter(Boolean).join(" ").toLowerCase();
    if (!terms.every(q => hay.includes(q))) return false;
  }
  return true;
}

function renderLibrary() {
  $$(".filter-group").forEach(g => {
    const key = g.dataset.key;
    $$(".fchip", g).forEach(c => c.classList.toggle("on", state.libFilters[key].has(c.textContent)));
  });
  const capAvg = a => Object.values(a.capability).reduce((sum, v) => sum + v, 0) / Object.keys(a.capability).length;
  const scaleRank = {"高位":3,"中位":2,"低位":1};
  const intakeRank = {open:3,tight:2,full:1};
  const sorters = {
    evidence: (a, b) => b.cases.length - a.cases.length || capAvg(b) - capAvg(a),
    capability: (a, b) => capAvg(b) - capAvg(a) || b.cases.length - a.cases.length,
    scale: (a, b) => scaleRank[b.scaleTier] - scaleRank[a.scaleTier] || b.cases.length - a.cases.length,
    intake: (a, b) => intakeRank[b.intake] - intakeRank[a.intake] || b.cases.length - a.cases.length
  };
  const list = AGENTS.filter(libMatch).sort(sorters[state.libSort]);
  $("#libCount").textContent = list.length + " / " + AGENTS.length + " 家";
  $("#libStats").innerHTML = 
    '<span>优质认证 <b>' + AGENTS.filter(a=>a.certs.youzhi).length + '</b></span>' +
    '<span>灵犀认证 <b>' + AGENTS.filter(a=>a.certs.lingxi).length + '</b></span>';

  $("#libGrid").innerHTML = list.map(a => {
    const ti = TRACKS[a.track];
    const leadCase = a.cases[0];
    const aboveMedian = Object.values(a.capability).filter(v => v >= 65).length;
    return '<div class="lib-card v9-card" style="--tk-color:' + ti.color + '" data-id="' + a.id + '">' +
      '<div class="lc-top"><div>' +
      '<div class="lc-name">' + a.name + '<span class="track-tag">' + a.track + '</span></div>' +
      '<div class="lc-hq">' + a.hq + ' · 优质上榜代理 · ' + a.industries.slice(0,3).join(' / ') + '</div>' +
      '</div></div>' +
      '<div class="lc-evidence"><div class="lce-head"><span>代表案例</span><em>' + a.cases.length + ' 条案例证据</em></div>' +
      '<b>' + leadCase.brand + ' · ' + leadCase.category + '</b><p>' + leadCase.result + '</p>' +
      '<div class="lce-meta"><span class="case-type-tag">' + leadCase.caseType + '</span><span>' + leadCase.industry + '</span><span>' + leadCase.objective + '</span></div></div>' +
      '<div class="lc-capability"><svg viewBox="0 0 160 146" aria-label="能力罗盘与大盘中位线">' + radarSvg([a], 80, 72, 48, true) + '</svg>' +
      '<div class="lc-cap-summary"><b>六维能力罗盘</b><span>' + aboveMedian + '/6 维达到或高于大盘中位线</span><div>' +
      Object.entries(a.capability).sort((x,y)=>y[1]-x[1]).slice(0,3).map(([k,v]) => '<i>' + CAP_NAMES[k] + ' · ' + grade(v) + '</i>').join('') +
      '</div></div></div>' +
      '<div class="lc-position"><span><i>体量站位</i><b>' + scaleLabel(a.scaleTier) + '</b></span><span><i>擅长目的</i><b>' + a.objectives.slice(0,2).join(' / ') + '</b></span></div>' +
      '<div class="cert-row">' + a.tags.slice(0, 3).map(t => '<span class="tag-chip">' + t + '</span>').join("") + '</div>' +
      '<div class="lc-actions">' +
      '<button class="btn btn-cy" data-detail="' + a.id + '">查看案例与档案</button>' +
      '<button class="btn" data-compare="' + a.id + '">' + (state.compare.has(a.id) ? "已在对比" : "+ 对比") + '</button>' +
      '</div></div>';
  }).join("");

  $$("#libGrid [data-detail]").forEach(b => b.onclick = e => { e.stopPropagation(); openDrawer(b.dataset.detail); });
  $$("#libGrid [data-compare]").forEach(b => b.onclick = e => { e.stopPropagation(); toggleCompare(b.dataset.compare); renderLibrary(); });
  $$("#libGrid .lib-card").forEach(c => c.onclick = () => openDrawer(c.dataset.id));

  const toMatchBtn = $("#libToMatch");
  if (toMatchBtn) toMatchBtn.onclick = () => {
    const f = state.libFilters;
    const brief = {
      industry: f.industry.size ? [...f.industry][0] : "母婴",
      category: "", stage: "新锐",
      objective: f.objective.size ? [...f.objective][0] : "种草效果化",
      budget: 30, form: "种草为主", special: "无", source: "新客首次代投",
      _missing: ["budget", "stage"], _fromLibrary: true
    };
    state.brief = brief;
    nav("match");
    $("#needInput").value = "（从站位库带入）" + brief.industry + "行业 · " + brief.objective + (f.track.size ? " · 限定类型：" + [...f.track].join("/") : "");
    renderBriefCard(brief);
    $("#briefCard").hidden = false;
    toast("已带入站位库筛选条件，补全预算与阶段后即可匹配");
  };
}

function openDrawer(id) {
  const a = AGENTS.find(x => x.id === id);
  const ti = TRACKS[a.track];
  let fitBlock = "";
  if (state.brief) {
    const b = state.brief;
    const r = matchAgent(a, b);
    const cc = competitorCheck(a, b);
    const briefLabel = b.industry + (b.category ? "·" + b.category : "") + " / " + b.stage + " / " + b.objective + " / 月耗" + b.budget + "w";
    if (!r.pass) {
      fitBlock = '<div class="fit-block fit-bad"><div class="fit-head">对当前商机的适配 <span>' + briefLabel + '</span></div><p>🚫 硬条件不适配：' + r.deadReasons.join("；") + '</p></div>';
    } else {
      const rs = buildReasons(r, b)[0];
      fitBlock = '<div class="fit-block ' + (r.total >= 55 ? "fit-good" : "fit-mid") + '"><div class="fit-head">对当前商机的适配 <span>' + briefLabel + '</span></div>' +
        '<div class="fit-score"><b>' + r.total + '</b><i>匹配分</i>' +
        '<div class="scorebar"><i class="sb-case" style="width:' + r.weighted.case + '%"></i><i class="sb-cert" style="width:' + r.weighted.cert + '%"></i><i class="sb-cap" style="width:' + r.weighted.cap + '%"></i><i class="sb-budget" style="width:' + r.weighted.budget + '%"></i></div></div>' +
        '<p>' + rs.icon + ' ' + rs.text + '</p>' +
        (cc.conflict ? '<p class="fit-warn">⚠ 竞品提示：在服品牌 ' + cc.brands.map(x => x.brand + "（" + x.cat + "）").join("、") + ' 与客户同品类，路由时渠道将重点核对</p>' : "") +
        '</div>';
    }
  }

  $("#drawerInner").innerHTML = 
    '<button class="dw-close" id="dwClose">✕</button>' +
    '<div class="dw-head"><div class="dw-name">' + a.name + '<span class="track-tag" style="--tk-color:' + ti.color + ';background:' + ti.color + '">' + a.track + '</span>' + intakeBadge(a) + '</div>' +
    '<div class="dw-sub">' + a.hq + ' · ' + a.founded + '年成立 · 团队 ' + a.teamSize + ' 人（小红书业务线 ' + a.redTeam + ' 人） · ' + a.intro + '</div></div>' +
    fitBlock +
    '<div class="dw-kpis">' +
    '<div class="dw-kpi"><b>' + scaleLabel(a.scaleTier) + '</b><span>消耗规模</span></div>' +
    '<div class="dw-kpi"><b>' + a.clientCount + '</b><span>在服客户数</span></div>' +
    '<div class="dw-kpi"><b>' + a.renewRate + '%</b><span>客户续约率</span></div>' +
    '<div class="dw-kpi"><b>' + a.selfOpRate + '%</b><span>自运营占比</span></div></div>' +
    '<div class="dw-section"><h3>认证与标签</h3><div class="cert-row">' +
    '<span class="cert ' + (a.certs.youzhi ? "cert-on" : "cert-off") + '">优质代理认证</span>' +
    '<span class="cert ' + (a.certs.lingxi ? "cert-on" : "cert-off") + '">灵犀认证</span>' +
    a.tags.map(t => '<span class="tag-chip">' + t + '</span>').join("") +
    '</div></div>' +
    '<div class="dw-section"><h3>能力罗盘</h3><div class="dw-radar-wrap">' +
    '<svg viewBox="0 0 260 240">' + radarSvg([a], 130, 120, 80, true) + '</svg>' +
    '<div class="dw-cap-list">' + Object.entries(CAP_NAMES).map(e => '<span>' + e[1] + ' <b class="grade-' + grade(a.capability[e[0]]) + '">' + grade(a.capability[e[0]]) + ' · ' + a.capability[e[0]] + '</b></span>').join("") + '</div></div></div>' +
    '<div class="dw-section"><h3>服务线与资源配置</h3><div class="cert-row">' + (a.svc || []).map(s => '<span class="tag-chip">' + s + '</span>').join("") + '</div>' +
    '<div class="dw-res"><b>工具能力</b>' + a.tools + '</div><div class="dw-res"><b>达人资源</b>' + a.kol + '</div></div>' +
    '<div class="dw-section"><h3>案例证据（' + a.cases.length + '）</h3>' +
    a.cases.map(c => '<div class="dw-case"><b>' + c.brand + ' · ' + c.category + '</b>' + c.result + '<div class="dc-meta">' + c.industry + ' / ' + c.stage + '品牌 / ' + c.objective + ' / ' + BUDGET_TIERS[c.tier] + ' / ' + c.period + (c.caseType ? ' / <span class="case-type-tag">' + c.caseType + '</span>' : "") + '</div></div>').join("") +
    '</div>' +
    '<div class="dw-section"><h3>服务品牌</h3><div class="dw-brands">' + a.clients.map(c => '<span class="tag-chip">' + c + '</span>').join("") + '</div></div>' +
    '<div class="dw-section"><h3>擅长营销目的 / 品牌阶段 / 预算区间</h3><div class="cert-row">' +
    a.objectives.map(o => '<span class="tag-chip">' + o + '</span>').join("") +
    a.brandStages.map(s => '<span class="tag-chip">' + s + '品牌</span>').join("") +
    '<span class="tag-chip">月耗 ' + a.budgetFit[0] + '-' + a.budgetFit[1] + 'w</span></div></div>' +
    '<div class="dw-actions"><button class="btn btn-cy" id="dwCompare">' + (state.compare.has(a.id) ? "已在对比栏" : "＋ 加入对比") + '</button>' +
    '<button class="btn btn-primary" id="dwMatch">以此代理反查适配需求</button></div>';
  
  $("#drawer").hidden = false; $("#drawerMask").hidden = false;
  $("#dwClose").onclick = closeDrawer;
  $("#dwCompare").onclick = () => { toggleCompare(a.id); $("#dwCompare").textContent = state.compare.has(a.id) ? "已在对比栏" : "＋ 加入对比"; };
  $("#dwMatch").onclick = () => { closeDrawer(); toast(a.name + " 擅长：" + a.objectives.join("、") + " · " + a.industries.slice(0,3).join("/")); };
}
function closeDrawer() { $("#drawer").hidden = true; $("#drawerMask").hidden = true; }

function setMatchStep(n) {
  $$(".match-step").forEach((el, i) => el.hidden = (i + 1 !== n));
  $$("#matchSteps .ms").forEach(s => {
    const v = +s.dataset.ms;
    s.classList.toggle("active", v === n);
    s.classList.toggle("done", v < n);
  });
}

function loadScenario(id) {
  const s = SCENARIOS.find(x => x.id === id);
  state.scenario = s;
  $("#needInput").value = s.input;
  onParse();
  toast("已载入：" + s.chip);
}

function onParse() {
  const text = $("#needInput").value.trim();
  if (!text) { toast("请先输入客户需求"); return; }
  const brief = parseBrief(text);
  state.brief = brief;
  state.scenario = SCENARIOS.find(s => s.id === brief._scenario) || state.scenario;
  renderBriefCard(brief);
  $("#briefCard").hidden = false;
  $("#briefCard").scrollIntoView({ behavior: "smooth", block: "center" });
}

const BRIEF_FIELDS = [
  { key:"industry", label:"行业", opts:["母婴","美妆","食品饮料","服饰潮流","家清个护","数码家电","汽车出行","本地生活"] },
  { key:"category", label:"品类", input:true },
  { key:"stage", label:"品牌阶段", opts:["新锐","成长","成熟"] },
  { key:"objective", label:"营销目的", opts:["新品冷启动","种草效果化","小预算试水","竞价效果","品牌心智"] },
  { key:"budget", label:"月预算（万元）", input:true, num:true },
  { key:"form", label:"投放形态", opts:["种草为主","种收一体","竞价为主","种草试水"] },
  { key:"special", label:"特殊要求", input:true }
];

function renderBriefCard(brief) {
  const grid = $("#briefGrid");
  const missing = brief._missing || [];
  grid.innerHTML = BRIEF_FIELDS.map(f => {
    const miss = missing.includes(f.key);
    const ctrl = f.opts
      ? '<select data-key="' + f.key + '">' + f.opts.map(o => '<option ' + (o === brief[f.key] ? "selected" : "") + '>' + o + '</option>').join("") + '</select>'
      : '<input data-key="' + f.key + '" value="' + (brief[f.key] != null ? brief[f.key] : "") + '"' + (f.num ? ' inputmode="numeric"' : "") + '>';
    return '<div class="brief-field ' + (miss ? "bf-missing" : "") + '"><label>' + f.label + (miss ? '<i class="bf-flag">未识别 · 请确认</i>' : "") + '</label>' + ctrl + '</div>';
  }).join("");
  grid.oninput = e => {
    const k = e.target.dataset.key; if (!k) return;
    state.brief[k] = k === "budget" ? (parseInt(e.target.value, 10) || 0) : e.target.value;
    if (state.brief._missing) {
      state.brief._missing = state.brief._missing.filter(x => x !== k);
      const field = e.target.closest(".brief-field");
      if (field) { field.classList.remove("bf-missing"); const fl = field.querySelector(".bf-flag"); if (fl) fl.remove(); }
    }
  };
}

function onConfirmBrief() {
  const missing = state.brief._missing || [];
  if (missing.length) { toast("还有 " + missing.length + " 个字段未确认（黄色标记），确认后再匹配"); return; }
  state.picked.clear(); state.routeStage = 0; state.routeDecision = null; state.timeline = []; state.handoffDone = false;
  state.match = runMatch(state.brief);
  renderMatchResult();
  setMatchStep(2);
  window.scrollTo({ top: 0, behavior: "smooth" });
}

const QL_INDUSTRIES = ["母婴","美妆","食品饮料","家清个护","服饰潮流","数码家电","汽车出行","本地生活"];

function renderQuickLookup() {
  $("#qlIndustries").innerHTML = QL_INDUSTRIES.map(ind => {
    const n = AGENTS.filter(a => a.industries.includes(ind)).length;
    return '<button class="ql-chip" data-ind="' + ind + '"' + (n === 0 ? " disabled" : "") + '>' + ind + '<b>' + n + '家</b></button>';
  }).join("");
  $$("#qlIndustries .ql-chip").forEach(b => b.onclick = () => quickLookup(b.dataset.ind));
}

function quickLookup(ind) {
  $$("#qlIndustries .ql-chip").forEach(b => b.classList.toggle("on", b.dataset.ind === ind));
  const list = AGENTS.filter(a => a.industries.includes(ind));
  const byTrack = {};
  list.forEach(a => { (byTrack[a.track] = byTrack[a.track] || []).push(a); });
  const caseCount = a => a.cases.filter(c => c.industry === ind).length;
  Object.values(byTrack).forEach(arr => arr.sort((x, y) => caseCount(y) - caseCount(x) || y.capability.scale - x.capability.scale));

  const el = $("#qlResult");
  el.hidden = false;
  el.innerHTML = '<div class="ql-grid">' + Object.entries(byTrack).map(e => {
    const tk = e[0], arr = e[1];
    return '<div class="ql-track" style="--tk-color:' + TRACKS[tk].color + '"><h4>' + tk + '<span>' + arr.length + '家</span></h4>' +
      arr.map(a => '<button class="ql-agent" data-open="' + a.id + '"><span class="qa-name">' + a.name + '</span><span class="qa-meta">' + (caseCount(a) ? "📌 " + ind + "案例×" + caseCount(a) : "行业覆盖") + " · " + (a.certs.youzhi ? "优质" : "灵犀") + '</span>' + intakeBadge(a) + '</button>').join("") + '</div>';
  }).join("") + '</div>' +
  '<div class="ql-actions"><button class="btn btn-cy" id="qlAnswerBtn">生成应答话术</button><button class="btn" id="qlDeepBtn">需求更具体？进入智能匹配 →</button></div>' +
  '<div class="answer-card" id="qlAnswer" hidden></div>';

  $$("#qlResult [data-open]").forEach(b => b.onclick = () => openDrawer(b.dataset.open));
  $("#qlDeepBtn").onclick = () => nav("match");
  $("#qlAnswerBtn").onclick = () => {
    const ac = $("#qlAnswer");
    const internal = list.slice().sort((x, y) => caseCount(y) - caseCount(x)).slice(0, 3);
    const intakeMap = {open:"可接",tight:"档期紧张",full:"满载"};
    const text = "【" + ind + "行业优质代理站位速览】（内部参考）\n重点推荐（按" + ind + "案例证据排序）：\n" +
      internal.map((a, i) => (i+1) + ". " + a.name + "（" + a.track + "）— " + (caseCount(a) ? ind + "案例×" + caseCount(a) : "行业覆盖") + "，" + (a.certs.youzhi ? "优质代理" : "灵犀认证") + "，消耗规模" + scaleLabel(a.scaleTier) + "，当前" + intakeMap[a.intake]).join("\n") +
      "\n\n如需对客推荐：本清单为内部优质代理比选参考，对客输出请转商服按标准推荐流程补齐复核。";
    ac.hidden = false;
    ac.innerHTML = '<div class="ac-head"><b>应答话术</b><span>内部版 · 含排序与依据 · 对客推荐请转商服流程</span><button class="btn btn-sm btn-cy" id="qlCopyBtn">复制</button></div><pre>' + text + '</pre>';
    $("#qlCopyBtn").onclick = () => {
      navigator.clipboard && navigator.clipboard.writeText ? navigator.clipboard.writeText(text).then(() => toast("已复制")).catch(() => toast("复制失败")) : toast("复制失败");
    };
    logSession("行业速查应答：<b>" + ind + " · " + list.length + "家</b>（渠道应答标准化，即答即沉淀）");
  };
}

function renderMatchResult() {
  const brief = state.brief, match = state.match;
  $("#briefSummary").innerHTML = [
    "行业 <b>" + brief.industry + (brief.category ? "·" + brief.category : "") + "</b>",
    "阶段 <b>" + brief.stage + "</b>", "目的 <b>" + brief.objective + "</b>", "月耗 <b>" + brief.budget + "w</b>",
    brief.special && brief.special !== "无" ? "要求 <b>" + brief.special + "</b>" : ""
  ].filter(Boolean).map(t => '<span>' + t + '</span>').join("");

  const note = $("#laneNote");
  if (match.alive.length <= 3) {
    note.hidden = false;
    note.innerHTML = '⚠️ 当前需求下适配代理仅 <b>' + match.alive.length + '</b> 家。本池为优质代理内部比选清单，若对客输出请转商服按标准推荐流程补齐复核。';
  } else note.hidden = true;

  const top3 = match.alive.filter(r => r.total >= 55).slice(0, 3);
  const dualRoute = (state.scenario && state.scenario.dualRoute) || {};

  $("#podium").innerHTML = top3.map((r, i) => {
    const a = r.agent, ti = TRACKS[a.track];
    const reasons = buildReasons(r, brief);
    const first = reasons[0];
    const cc = competitorCheck(a, brief);
    return '<div class="podium-card p' + (i+1) + '">' +
      '<span class="p-rank">' + ["① 最高匹配","② 次高匹配","③ 备选"][i] + '</span>' +
      '<div class="p-head"><div class="p-name"><span data-open="' + a.id + '" style="cursor:pointer">' + a.name + '</span>' +
      '<span class="track-tag" style="background:' + ti.color + '">' + a.track + '</span>' +
      (dualRoute[a.id] ? '<span class="route-tag">' + dualRoute[a.id].split(" · ")[0] + '</span>' : "") + '</div>' +
      '<span class="p-score">' + r.total + '</span></div>' +
      intakeBadge(a) + (a.intake === "full" ? '<span style="font-size:11px;color:var(--tx-3)">分数≠可接，看渠道终审</span>' : "") +
      '<div class="evidence ' + (first.strong ? "" : "ev-weak") + '"><div class="ev-label">类似客户证据</div><p>' + first.text + '</p>' +
      (reasons[1] && reasons[1].strong ? '<div class="ev-sub">' + reasons[1].icon + ' ' + reasons[1].text + '</div>' : "") + '</div>' +
      (cc.conflict ? '<div class="conflict-hint">⚠ 在服品牌含同品类（' + cc.brands.map(x => x.brand).join("、") + '），渠道终审将核对竞品冲突</div>' : "") +
      '<div class="scorebar-wrap"><div class="scorebar"><i class="sb-case" style="width:' + r.weighted.case + '%"></i><i class="sb-cert" style="width:' + r.weighted.cert + '%"></i><i class="sb-cap" style="width:' + r.weighted.cap + '%"></i><i class="sb-budget" style="width:' + r.weighted.budget + '%"></i></div></div>' +
      '<div class="reason-list">' + reasons.slice(first.strong && reasons[1] && reasons[1].strong ? 2 : 1).map(x => '<div>' + x.icon + ' ' + x.text + '</div>').join("") + '</div>' +
      (dualRoute[a.id] ? '<div style="font-size:12px;color:var(--tx-2);border:1px dashed var(--line-strong);border-radius:8px;padding:6px 10px">🧭 ' + dualRoute[a.id] + '</div>' : "") +
      '<div class="pick-row"><label><input type="checkbox" data-pick="' + a.id + '"' + (state.picked.has(a.id) ? " checked" : "") + '> 勾选为意向代理</label>' +
      '<div class="mini-actions"><button class="mini-btn" data-open="' + a.id + '">档案</button><button class="mini-btn" data-cmp="' + a.id + '">+对比</button></div></div></div>';
  }).join("");

  const rest = match.alive.slice(top3.length);
  $$("#podium .p-score").forEach(el => countUp(el, parseInt(el.textContent, 10) || 0, "", 750));

  $("#restTitle").textContent = rest.length || match.dead.length
    ? "其余候选 " + rest.length + " 家 · 硬过滤出局 " + match.dead.length + " 家（置灰，附出局原因）" : "";

  $("#board").innerHTML = [...rest.map(r => restCard(r, false)), ...match.dead.map(r => restCard(r, true))].join("");

  $$("#page-match [data-pick]").forEach(cb => cb.onchange = e => {
    if (e.target.checked) state.picked.add(cb.dataset.pick); else state.picked.delete(cb.dataset.pick);
    updateSelectBar();
  });
  $$("#page-match [data-open]").forEach(b => b.onclick = () => openDrawer(b.dataset.open));
  $$("#page-match [data-cmp]").forEach(b => b.onclick = () => { toggleCompare(b.dataset.cmp); });
  updateSelectBar();
}

function restCard(r, dead) {
  const a = r.agent, ti = TRACKS[a.track];
  if (dead) return '<div class="agent-card dead"><div class="ac-top"><div class="ac-name" data-open="' + a.id + '">' + a.name + '<span class="track-tag" style="background:' + ti.color + '">' + a.track + '</span></div>' + intakeBadge(a) + '</div><div class="dead-reason">🚫 ' + r.deadReasons.join("；") + '</div></div>';
  const reasons = buildReasons(r, state.brief);
  return '<div class="agent-card"><div class="ac-top"><div class="ac-name" data-open="' + a.id + '">' + a.name + '<span class="track-tag" style="background:' + ti.color + '">' + a.track + '</span></div>' + intakeBadge(a) + '</div>' +
    '<div class="evidence ' + (reasons[0].strong ? "" : "ev-weak") + '"><div class="ev-label">类似客户证据</div><p>' + reasons[0].text + '</p></div>' +
    '<div class="scorebar-wrap"><div class="scorebar"><i class="sb-case" style="width:' + r.weighted.case + '%"></i><i class="sb-cert" style="width:' + r.weighted.cert + '%"></i><i class="sb-cap" style="width:' + r.weighted.cap + '%"></i><i class="sb-budget" style="width:' + r.weighted.budget + '%"></i></div><span class="score-num">' + r.total + '</span></div>' +
    '<div class="pick-row"><label><input type="checkbox" data-pick="' + a.id + '"' + (state.picked.has(a.id) ? " checked" : "") + '> 意向</label><div class="mini-actions"><button class="mini-btn" data-open="' + a.id + '">档案</button><button class="mini-btn" data-cmp="' + a.id + '">+对比</button></div></div></div>';
}

function updateSelectBar() {
  const n = state.picked.size;
  $("#selectInfo").textContent = n
    ? "已勾选 " + n + " 家：" + [...state.picked].map(id => AGENTS.find(a => a.id === id).name).join("、")
    : "尚未勾选意向代理（可先生成内部比选清单）";
  $("#toRouteBtn").disabled = n === 0;
  $("#addCompareBtn").disabled = n === 0;
}

function switchTab(name) {
  $$(".tab").forEach(t => t.classList.toggle("active", t.dataset.tab === name));
  $("#panel-compliance").hidden = name !== "compliance";
  $("#panel-route").hidden = name !== "route";
  if (name === "compliance") renderCompliance();
}

function renderCompliance() {
  const comp = buildShortlist(state.match, state.brief);
  const banner = $("#ruleBanner");
  banner.className = "rule-banner info";
  banner.innerHTML = '<b>📋 内部优质代理比选清单</b><span>类型覆盖 ' + comp.trackCount + ' 类 · 共 ' + comp.totalCount + ' 家 · 本清单为内部参考，对客输出请转商服按标准推荐流程补齐复核</span>';

  const hl = state.match.alive.filter(r => r.total >= 55).slice(0, 3).map(r => r.agent.id);
  $("#internalList").innerHTML = state.match.alive.map((r, i) => 
    '<li class="' + (hl.includes(r.agent.id) ? "is-hl" : "") + '"><span class="rank">' + (i+1) + '</span><span class="iname">' + r.agent.name + '</span>' +
    '<span class="inote">' + (r.parts.case >= 100 ? "案例完全命中" : r.parts.case >= 70 ? "相邻案例" : "行业覆盖") + '</span>' +
    '<span class="iscore">' + r.total + '</span></li>'
  ).join("");
  
  const ext = $("#externalList");
  ext.innerHTML = "";
  for (const e of Object.entries(comp.byTrack)) {
    const tk = e[0], arr = e[1];
    const div = document.createElement("div");
    div.className = "ext-track";
    div.style.setProperty("--tk-color", TRACKS[tk].color);
    const sorted = [...arr].sort((a, b) => a.agent.name.localeCompare(b.agent.name, "zh"));
    div.innerHTML = '<h4>' + tk + '（' + arr.length + ' 家）</h4><ul>' + sorted.map(r => 
      '<li><b>' + r.agent.name + '</b>' + r.agent.intro + '<span class="ext-res">服务线：' + (r.agent.svc || []).join(" / ") + ' · ' + r.agent.kol + '</span>' + (r.note ? '<br><i>备注：' + r.note + '</i>' : "") + '</li>'
    ).join("") + '</ul>';
    ext.appendChild(div);
  }
  $("#externalFoot").textContent = "本草稿为内部优质代理比选清单，不含对客排序。如需对客输出，请转商服按标准推荐流程补齐复核，平台不替客户做最终选择。";

  const hb = $("#handoffBtn"), hs = $("#handoffState");
  if (hb) {
    if (state.handoffDone) {
      hb.disabled = true; hb.textContent = "已流转商服 ✓";
      hs.textContent = "商服复核后将按标准流程对客发出";
    } else {
      hb.disabled = false; hb.textContent = "一键流转商服（标准流程）"; hs.textContent = "";
      hb.onclick = () => {
        state.handoffDone = true;
        logSession("内部比选清单流转商服：<b>" + comp.trackCount + "类型 · " + comp.totalCount + "家</b>（含Brief与匹配依据，商服零重复劳动）");
        renderCompliance();
        toast("已流转商服——清单草稿 + Brief + 匹配依据一并送达");
      };
    }
  }
}

let routeView = "da";
function startRoute() {
  if (!state.picked.size) return;
  if (state.routeStage === 0) {
    state.routeStage = 1;
    state.timeline = [
      { t: now(), title: "直客发起建联", desc: "勾选 " + pickedNames() + "，生成脱敏 Brief", cls: "tl-done" },
      { t: now(), title: "已路由至 渠道四组·母婴线", desc: "等待渠道确认承接", cls: "tl-now", tag: "＋1 条商机记录" }
    ];
  }
  renderRoute();
  $$(".view-toggle button").forEach(b => b.onclick = () => {
    routeView = b.dataset.view;
    $$(".view-toggle button").forEach(x => x.classList.toggle("active", x === b));
    renderRoute();
  });
}
function pickedNames() { return [...state.picked].map(id => AGENTS.find(a => a.id === id).name).join("、"); }
function now() { const d = new Date(); return String(d.getHours()).padStart(2,"0") + ":" + String(d.getMinutes()).padStart(2,"0"); }

function renderRoute() {
  const main = $("#routeMain"), b = state.brief;
  const pickedId = [...state.picked][0];
  const pickedAgent = AGENTS.find(a => a.id === pickedId);
  const script = state.scenario && state.scenario.routeScript;
  const r = state.match.alive.find(x => x.agent.id === pickedId);
  const briefBlock = '<div class="route-brief"><h3>建联 Brief（脱敏）<span>不含客户名称与联系方式 · 不含渠道经理个人信息</span></h3><dl>' +
    '<div><dt>行业/品类</dt><dd>' + b.industry + (b.category ? "·" + b.category : "") + '</dd></div>' +
    '<div><dt>品牌阶段</dt><dd>' + b.stage + '</dd></div>' +
    '<div><dt>营销目的</dt><dd>' + b.objective + '</dd></div>' +
    '<div><dt>月预算档</dt><dd>' + BUDGET_TIERS[budgetTier(b.budget)] + '</dd></div>' +
    '<div><dt>特殊要求</dt><dd>' + (b.special || "无") + '</dd></div></dl></div>';
  const statusPill = state.routeStage === 2
    ? (state.routeDecision === "conflict" ? '<span class="status-pill st-conflict">档期冲突 · 已建议换家</span>' : '<span class="status-pill st-ok">渠道已确认 · 可安排建联</span>')
    : '<span class="status-pill st-wait">待渠道确认</span>';
  const targetBlock = '<div class="route-target-card"><div>意向代理：<b>' + pickedAgent.name + '</b> <span style="font-size:12px;color:var(--tx-3)">（' + pickedAgent.track + '）</span></div>' + statusPill + '</div>';

  if (routeView === "da") {
    let decision;
    if (state.routeStage === 2 && state.routeDecision === "conflict" && script && script.altId) {
      const alt = AGENTS.find(a => a.id === script.altId);
      decision = '<div class="decision-note conflict"><b>渠道反馈：档期冲突</b>「' + script.reason + '」<div class="alt-suggest">💡 渠道建议改选 <b>' + alt.name + '</b>：' + script.altReason + '<br><button class="btn btn-cy" id="acceptAltBtn">接受建议，改选 ' + alt.name + '</button></div></div>';
    } else if (state.routeStage === 2) {
      decision = '<div class="decision-note ok"><b>渠道已确认承接</b>「' + ((script && script.note) || "可安排建联") + '」下一步：渠道协调代理商务与客户对接开户。</div>';
    } else {
      decision = '<div class="decision-note"><b>已路由，等待渠道确认</b>切换上方「渠道视角」查看渠道操作界面 →</div>';
    }
    main.innerHTML = briefBlock + targetBlock + decision;
    const altBtn = $("#acceptAltBtn");
    if (altBtn) altBtn.onclick = acceptAlt;
  } else {
    const reasons = r ? buildReasons(r, b) : [];
    const cc = competitorCheck(pickedAgent, b);
    let ccBlock;
    if (cc.conflict) ccBlock = '<div class="qu-conflict warn">⚠ <b>竞品核对</b>：' + pickedAgent.name + ' 在服品牌 ' + cc.brands.map(x => '<b>' + x.brand + '</b>（' + x.cat + '）').join("、") + ' 与客户同品类——请确认客户与代理双方是否接受，或选择换家。</div>';
    else if (cc.near && cc.near.length) ccBlock = '<div class="qu-conflict near">ℹ 相邻品类提示：在服品牌 ' + cc.near.map(x => x.brand + "（" + x.cat + "）").join("、") + '，非直接竞品，通常可接。</div>';
    else ccBlock = '<div class="qu-conflict ok">✓ 竞品核对：在服品牌与客户品类无冲突。</div>';
    let actions;
    if (state.routeStage < 2) {
      actions = ccBlock + '<div class="qu-reasons"><h4>系统初筛理由（供参考，最终判断在渠道）</h4><ul>' + reasons.slice(0, 3).map(x => '<li>' + x.text + '</li>').join("") + '</ul></div>' +
        '<div class="route-actions"><button class="btn btn-cy" id="acceptBtn">✓ 能接</button><button class="btn btn-gold" id="conflictBtn">▲ 档期冲突</button><button class="btn btn-dark-ghost" id="altBtn">⇄ 建议换家</button></div>' +
        '<p style="font-size:11.5px;color:var(--tx-3);margin-top:10px">每一次确认动作都会沉淀为结构化数据：承接状态、冲突原因、替代建议。</p>';
    } else {
      actions = state.routeDecision === "conflict"
        ? '<div class="decision-note conflict"><b>已反馈：档期冲突</b>「' + (script ? script.reason : "") + '」已建议直客改选 ' + (script && script.altId ? AGENTS.find(a => a.id === script.altId).name : "") + '。</div>'
        : '<div class="decision-note ok"><b>已确认承接</b>「' + ((script && script.note) || "") + '」</div>';
    }
    main.innerHTML = briefBlock + targetBlock + actions;
    const ab = $("#acceptBtn"), cb = $("#conflictBtn"), altb = $("#altBtn");
    const kind = (script && script.type === "conflict") ? "conflict" : "accept";
    if (ab) ab.onclick = () => decide(kind);
    if (cb) cb.onclick = () => decide("conflict");
    if (altb) altb.onclick = () => decide(kind);
  }
  $("#timeline").innerHTML = (state.timeline || []).map(x => 
    '<li class="' + x.cls + '"><b>' + x.title + '</b>' + x.desc + '<br><time>' + x.t + '</time>' + (x.tag ? ' <span class="tl-data-tag">' + x.tag + '</span>' : "") + '</li>'
  ).join("");
}

function decide(kind) {
  const script = (state.scenario && state.scenario.routeScript) || {};
  state.routeStage = 2; state.routeDecision = kind;
  if (kind === "conflict") {
    state.timeline.push({ t: now(), title: "渠道反馈：档期冲突", desc: script.reason || "当前档期无法承接", cls: "tl-now", tag: "＋承接状态数据" });
    if (script.altId) state.timeline.push({ t: now(), title: "建议换家：" + AGENTS.find(a => a.id === script.altId).name, desc: script.altReason || "", cls: "tl-now", tag: "＋替代建议数据" });
    logSession("承接状态更新：<b>" + AGENTS.find(a => a.id === [...state.picked][0]).name + " · Q4满载</b>（来自渠道确认动作）");
    if (script.altId) logSession("替代建议记录：<b>→ " + (AGENTS.find(a => a.id === script.altId) ? AGENTS.find(a => a.id === script.altId).name : "") + "</b>（含理由，可复盘）");
  } else {
    state.timeline.push({ t: now(), title: "渠道确认：能接", desc: script.note || "可安排建联", cls: "tl-now", tag: "＋承接状态数据" });
    logSession("承接确认：<b>" + pickedNames() + " · 能接</b>" + (script.note ? "（备注：" + script.note + "）" : ""));
  }
  state.timeline.forEach((x, i) => { if (i < state.timeline.length - 1) x.cls = "tl-done"; });
  renderRoute();
  toast(kind === "conflict" ? "渠道反馈已回流——切回直客视角看建议" : "渠道已确认，闭环完成");
}

function acceptAlt() {
  const script = state.scenario.routeScript;
  const alt = AGENTS.find(a => a.id === script.altId);
  state.picked = new Set([alt.id]);
  state.routeDecision = "accept";
  state.timeline.push({ t: now(), title: "直客确认改选 " + alt.name, desc: "进入建联，渠道协调代理商务对接", cls: "tl-now", tag: "＋命中率反馈数据" });
  state.timeline.forEach((x, i) => { if (i < state.timeline.length - 1) x.cls = "tl-done"; });
  logSession("最终选择回填：<b>" + alt.name + "</b>（初筛第2位被选中 → 反哺权重调优）");
  renderRoute();
  toast("已改选 " + alt.name + " · 本次路由沉淀 4 条数据 → 去「协同记录」看");
}

function logSession(html) {
  state.sessionLogs.push(html);
  $("#sessionLog").innerHTML = state.sessionLogs.map(h => '<li>' + h + '</li>').join("");
}

function toggleCompare(id) {
  if (state.compare.has(id)) { state.compare.delete(id); toast("已移出对比"); }
  else if (state.compare.size >= 3) { toast("最多对比 3 家"); return; }
  else { state.compare.add(id); toast("已加入对比（" + state.compare.size + "/3）· 左侧「代理对比」查看"); }
  if (state.page === "compare") renderCompare();
}

const RADAR_COLORS = ["#37d0c5", "#6ea8ff", "#ff6b6b"];

function renderCompare() {
  $("#comparePicker").innerHTML = AGENTS.map(a => 
    '<button class="cp-chip ' + (state.compare.has(a.id) ? "on" : "") + '" data-id="' + a.id + '">' + a.name + '</button>'
  ).join("");
  $$("#comparePicker .cp-chip").forEach(c => c.onclick = () => { toggleCompare(c.dataset.id); renderCompare(); });

  const list = [...state.compare].map(id => AGENTS.find(a => a.id === id));
  const has = list.length >= 2;
  $("#compareEmpty").hidden = has;
  $("#compareArea").hidden = !has;
  if (!has) return;

  $("#radarSvg").innerHTML = radarSvg(list, 180, 160, 120, true);
  $("#radarLegend").innerHTML = list.map((a, i) => '<span><i style="background:' + RADAR_COLORS[i] + '"></i>' + a.name + '</span>').join("");

  const rows = [
    ["类型", a => a.track, false],
    ["消耗规模", a => scaleLabel(a.scaleTier), false],
    ["在服客户数", a => a.clientCount, true],
    ["客户续约率", a => a.renewRate, true, v => v + "%"],
    ["自运营占比", a => a.selfOpRate, true, v => v + "%"],
    ["红书线团队", a => a.redTeam, true, v => v + "人"],
    ["预算区间", a => a.budgetFit[0] + "-" + a.budgetFit[1] + "w", false],
    ["优质代理", a => a.certs.youzhi ? "✓ 已认证" : "—", false],
    ["灵犀认证", a => a.certs.lingxi ? "✓ 已认证" : "—", false],
    ["承接状态", a => ({open:"● 可接",tight:"▲ 紧张",full:"■ 满载"})[a.intake], false],
    ["服务线", a => (a.svc || []).join("、"), false],
    ["工具能力", a => a.tools, false],
    ["达人资源", a => a.kol, false]
  ];
  $("#compareTable").innerHTML = 
    '<tr><th></th>' + list.map((a, i) => '<th style="color:' + RADAR_COLORS[i] + '">' + a.name + '</th>').join("") + '</tr>' +
    rows.map(row => {
      const label = row[0], fn = row[1], numeric = row[2], fmt = row[3];
      const vals = list.map(fn);
      const best = numeric ? Math.max(...vals) : null;
      return '<tr><td>' + label + '</td>' + vals.map(v => '<td class="' + (numeric && v === best ? "best" : "") + '"><b>' + (fmt ? fmt(v) : v) + '</b></td>').join("") + '</tr>';
    }).join("");

  $("#compareCases").innerHTML = list.map((a, i) => 
    '<div class="cc-col"><h4 style="color:' + RADAR_COLORS[i] + '">' + a.name + '（' + a.cases.length + ' 条案例）</h4>' +
    a.cases.map(c => '<div class="case-item"><b>' + c.brand + ' · ' + c.category + '</b>' + c.result + '<div class="ci-meta">' + c.industry + ' / ' + c.stage + ' / ' + c.objective + ' / ' + BUDGET_TIERS[c.tier] + (c.caseType ? ' / <span class="case-type-tag">' + c.caseType + '</span>' : "") + '</div></div>').join("") +
    '</div>'
  ).join("");

  const exit = $("#compareExit");
  if (exit) {
    exit.innerHTML = '<div class="exit-bar"><span>比完了？直接选定意向代理，进入建联路由：</span>' +
      list.map((a, i) => '<button class="btn btn-cy" data-pickgo="' + a.id + '" style="border-color:' + RADAR_COLORS[i] + ';color:' + RADAR_COLORS[i] + '">选定 ' + a.name + ' →</button>').join("") + '</div>';
    $$("#compareExit [data-pickgo]").forEach(btn => btn.onclick = () => {
      const id = btn.dataset.pickgo;
      if (!state.match) { nav("match"); toast("先确认客户 Brief，再路由这家代理"); return; }
      state.picked = new Set([id]);
      nav("match"); setMatchStep(3); switchTab("route"); state.routeStage = 0; startRoute();
      toast("已选定 " + AGENTS.find(a => a.id === id).name + "，进入建联路由");
    });
  }
}

function radarSvg(agents, cx, cy, R, showMedian) {
  const dims = Object.keys(CAP_NAMES);
  const angle = i => (-90 + i * 60) * Math.PI / 180;
  const pt = (i, v) => [cx + R * v / 100 * Math.cos(angle(i)), cy + R * v / 100 * Math.sin(angle(i))];
  let out = "";
  [25, 50, 75, 100].forEach(lv => {
    const pts = dims.map((_, i) => pt(i, lv).join(",")).join(" ");
    out += '<polygon points="' + pts + '" fill="none" stroke="rgba(148,180,220,.14)" stroke-width="' + (lv === 100 ? 1 : .5) + '"/>';
  });
  if (showMedian) {
    const medianVal = 65;
    const pts = dims.map((_, i) => pt(i, medianVal).join(",")).join(" ");
    out += '<polygon points="' + pts + '" fill="none" stroke="rgba(232,150,12,.45)" stroke-width="1.2" stroke-dasharray="4 3"/>';
    const lx2 = pt(0, 118)[0], ly2 = pt(0, 118)[1];
    out += '<text x="' + lx2 + '" y="' + (ly2 + 2) + '" text-anchor="middle" font-size="9" fill="rgba(232,150,12,.7)">中位线</text>';
  }
  dims.forEach((d, i) => {
    const xy = pt(i, 100);
    out += '<line x1="' + cx + '" y1="' + cy + '" x2="' + xy[0] + '" y2="' + xy[1] + '" stroke="rgba(148,180,220,.14)" stroke-width=".5"/>';
    const lxy = pt(i, 122);
    out += '<text x="' + lxy[0] + '" y="' + (lxy[1] + 3) + '" text-anchor="middle" font-size="10.5" fill="#9fb0c5">' + CAP_NAMES[d] + '</text>';
  });
  agents.forEach((a, ai) => {
    const color = agents.length === 1 ? "#37d0c5" : RADAR_COLORS[ai];
    const pts = dims.map((d, i) => pt(i, a.capability[d]).join(",")).join(" ");
    out += '<polygon points="' + pts + '" fill="' + color + '" fill-opacity=".14" stroke="' + color + '" stroke-width="2"/>';
    dims.forEach((d, i) => { const xy = pt(i, a.capability[d]); out += '<circle cx="' + xy[0] + '" cy="' + xy[1] + '" r="2.5" fill="' + color + '"/>'; });
  });
  return out;
}

function drawFlywheel() {
  const svg = $("#flywheelSvg");
  if (!svg) return;
  const cx = 210, cy = 210, R = 148;
  const nodes = [
    { a:-90, label:"客户需求", sub:"结构化 Brief" },
    { a:-18, label:"优质代理初筛", sub:"资质 · 行业 · 案例" },
    { a:54, label:"渠道终审", sub:"承接 · 冲突 · 换家", hl:true },
    { a:126, label:"结果回流", sub:"状态 · 命中 · 案例", hl:true },
    { a:198, label:"站位更新", sub:"下一次判断参考" }
  ];
  const pos = a => [cx + R*Math.cos(a*Math.PI/180), cy + R*Math.sin(a*Math.PI/180)];
  let out = '<defs><marker id="arrowhead" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto"><polygon points="0 0, 7 3.5, 0 7" fill="#64748c"/></marker></defs>';
  nodes.forEach((n,i) => {
    const next=nodes[(i+1)%nodes.length];
    const a1=(n.a+14)*Math.PI/180, a2=(next.a-14)*Math.PI/180;
    out += '<path class="fw-arrow" d="M '+(cx+R*Math.cos(a1))+' '+(cy+R*Math.sin(a1))+' A '+R+' '+R+' 0 0 1 '+(cx+R*Math.cos(a2))+' '+(cy+R*Math.sin(a2))+'"/>';
  });
  nodes.forEach(n => {
    const [x,y]=pos(n.a);
    out += '<g><circle class="'+(n.hl?'fw-node-hl':'fw-node')+'" cx="'+x+'" cy="'+y+'" r="46"/><text class="fw-label" x="'+x+'" y="'+(y-2)+'" text-anchor="middle">'+n.label+'</text><text class="fw-sub" x="'+x+'" y="'+(y+14)+'" text-anchor="middle">'+n.sub+'</text></g>';
  });
  out += '<text class="fw-center-t" x="'+cx+'" y="'+(cy-4)+'" text-anchor="middle">确认即沉淀</text><text class="fw-center-s" x="'+cx+'" y="'+(cy+14)+'" text-anchor="middle">不额外增加渠道填表</text>';
  svg.innerHTML = out;
}

function renderInsights() {
  $("#sessionLog").innerHTML = state.sessionLogs.length
    ? state.sessionLogs.map(h => '<li>' + h + '</li>').join("")
    : '<li class="empty">完成一次建联路由后，这里会出现本次操作沉淀的数据条目</li>';
  drawFlywheel();
}

function toast(msg) {
  const t = document.createElement("div");
  t.className = "toast"; t.textContent = msg;
  $("#toastWrap").appendChild(t);
  setTimeout(() => t.remove(), 2800);
}

function init() {
  $$(".nav-list button").forEach(b => b.onclick = () => nav(b.dataset.page));
  $("#drawerMask").onclick = closeDrawer;
  document.addEventListener("keydown", e => { if (e.key === "Escape") closeDrawer(); });

  const heroGo = () => {
    const v = $("#heroInput").value.trim();
    nav("match");
    if (v) { $("#needInput").value = v; onParse(); }
  };
  $("#heroGo").onclick = heroGo;
  $("#heroInput").addEventListener("keydown", e => { if (e.key === "Enter") heroGo(); });

  $("#quickFill").innerHTML = SCENARIOS.map(s => '<button data-sc="' + s.id + '">' + s.icon + ' ' + s.chip + '</button>').join("");
  $$("#quickFill button").forEach(b => b.onclick = () => loadScenario(b.dataset.sc));
  $("#parseBtn").onclick = onParse;
  $("#confirmBriefBtn").onclick = onConfirmBrief;
  $("#backToBrief").onclick = () => setMatchStep(1);
  $("#backToMatrix").onclick = () => setMatchStep(2);
  $("#toComplianceBtn").onclick = () => { setMatchStep(3); switchTab("compliance"); };
  $("#toRouteBtn").onclick = () => { setMatchStep(3); switchTab("route"); startRoute(); };
  $("#addCompareBtn").onclick = () => { [...state.picked].slice(0, 3).forEach(id => { if (!state.compare.has(id) && state.compare.size < 3) state.compare.add(id); }); toast("已把勾选代理加入对比（" + state.compare.size + "/3）"); };
  $$(".tab").forEach(t => t.onclick = () => {
    switchTab(t.dataset.tab);
    if (t.dataset.tab === "route" && state.routeStage === 0 && state.picked.size) startRoute();
  });
  $("#clearCompare").onclick = () => { state.compare.clear(); renderCompare(); };

  initFilters();
  renderHome();
  renderQuickLookup();
  renderInsights();
  nav("home");
}
init();
</script>


</body></html>
